# 🚀 智题库 (TiKu) 升级日记与架构演进

这个项目从刚诞生时一个基础的“在线考试小工具”，一步步成长为**“能让成千上万家企业同时使用、带 AI 助手的通用管理平台”**。

下面是我们从 v1.0 到 v1.4 每次大升级的详细总结，包括我们用了什么技术、踩了什么坑、以及前端 UI 和底层接口是如何步步进化的。

---

## 🌟 [v1.4] 彻底破圈：SaaS 多租户架构转型 - **正在进行**
> **目标**：我们把系统里所有的“老师、学生、考试”等字眼全部抹掉，改成“管理员、成员、任务”。这意味着它正式摆脱“教育场景”的桎梏，演化为一个通用型 B2B 企业管理底座。

- **🏢 底层重构 (多租户基石)**：这是在数据库层面动刀最大的一次。我们推翻了旧表，引入了 `sys_tenant` (租户表) 和强制的 `tenant_id` 外键隔离。无论是在查询资源还是下发任务时，底层 SQLAlchemy 模型都会自动过滤，确保 A 企业绝对看不到 B 企业的数据，实现物理级的数据防泄漏。
- **🚪 UI 交互 (极简入驻流)**：废弃了 C 端的开放注册页面。前端管理后台新增了“静默录入”功能：管理员输入成员手机号，后端触发 UPSERT 逻辑自动建号并绑定组织，彻底消灭了以往“账号建了却不知道归属哪家公司”的悬空状态。
- **🧠 架构升级 (知识库分权)**：在存储结构上，我们将知识库彻底剥离为 `Public`（全企业共享的合规标准）和 `Private`（个人的私密笔记）。
- **🕵️ 信任机制 (AI 深度溯源)**：为了解决大模型的“幻觉”问题，我们强制要求 AI 接口在返回内容时，必须在 JSON 中附带 `ai_rag_sources`。前端 UI 解析后，会高亮显示该结论引用了哪份文件的第几段，建立起企业级的使用信任。

---

## ⚡ [v1.3] 权限边界与云原生架构
> **目标**：建立森严的等级访问制度，并完成全栈的 Serverless（无服务器）云原生改造，实现零成本运维。

- **🛡️ 权限引擎 (严格 RBAC)**：后端在所有的 API 路由前挂载了角色鉴权依赖，前端则配合 Vue Router 的全局路由守卫拦截。超级管理员拥有“上帝面板”，而普通管理员只能在自己的组织圈子里活动，越权访问会被直接弹回。
- **☁️ 运维重构 (云端免卡部署)**：我们打通了极其前沿的“双擎驱动”模式。本地开发用 Docker + MySQL，而线上生产环境则一键白嫖了全球边缘网络：
  - **后端 API** 托管在了 **Vercel** 上。
  - **数据库** 迁移到了兼容 MySQL 8.0 的 **TiDB Cloud (无服务器版)**。
  - **前端 B/C 端** 双双挂载于 **Cloudflare Pages**。
- **📊 视觉体验 (SaaS 级大盘)**：前端基于 ECharts 1:1 像素级还原了一套高级商业数据大屏，可以实时监控当前组织的考试通过率、人员活跃度等核心指标。

---

## 🧠 [v1.2] 物理双端拆分与 AI 深度介入
> **目标**：为了不同场景的用户体验，强行把系统劈成两半，并深度整合大模型，将判卷和出题工作自动化。

- **🧩 工程拆分 (前端物理隔离)**：
  - **B端 (管理后台 `tob`)**：采用 `Vue 3 + Element Plus`。重点设计了极具科技感的“阅卷大厅”和“消息中心”页面，支持高密度的表格操作。
  - **C端 (移动端 `toc`)**：采用 `Vue 3 + H5`。引入了极其流畅的底部 TabBar、答题卡滑块以及用纯 SVG 手绘的环形得分动画，完全贴合手机端习惯。
- **🤖 算力引擎 (BackgroundTasks 异步阅卷)**：在后端引入了 4 级 Failover 轮询熔断机制对接商汤大模型。交卷不卡顿（直接返回 200），后端 FastAPI 利用后台任务在闲时异步调用 AI 逐条批阅简答题。
- **🗂️ Copilot 交互 (AI 抽屉)**：在前端开发了一个全局悬浮的 AI Copilot 组件，支持类似 Doubao 的 Markdown 流式回复和 Action 卡片，管理员能在对话中直接勾选题目并一键插入数据库。
- **📑 测试与校验**：新增了端到端 (E2E) 测试脚本 `e2e_selftest.py`，模拟真实用户注册、登录、半对错选、甚至触发真实 AI 批阅，多达 47 项用例全绿通过后才允许发版。

---

## 🔒 [v1.1] 悲观锁防御与状态机重塑
> **目标**：彻底消灭由于网络卡顿或野蛮操作导致的数据崩溃、成绩丢失等恶性 Bug。

- **⚔️ 数据库并发防御 (行级悲观锁)**：在 `exam_service.py` 核心提交逻辑中，我们强行注入了 `FOR UPDATE` 行级死锁。就算网络卡顿导致几十个“交卷”请求同时到达，数据库也会排队处理，算分一次后立即阻断后续请求，成绩再也不会被覆盖。
- **🗑️ 架构设计 (状态机流转)**：废弃了粗暴的物理删除操作，改为 `Draft`(草稿) -> `Published`(上架) -> `Archived`(归档) 三态流转。前端 UI 同步跟进，一旦试卷上架，编辑按钮变灰锁定，彻底解决了“有人正在做题，题目却被管理员删掉导致全服 500 报错”的惨剧。
- **👻 性能优化 (幽灵记录惰性清理)**：摒弃了耗费服务器性能的后台轮询任务。采用了极其讨巧的“惰性收卷”策略：当用户下一次登录或打开列表时，后端在查询阶段顺手将他上次超时的残卷结算掉，完全不占用额外的运维资源。

---

## 🍼 [v1.0] MVP 单体雏形与地基搭建
> **目标**：从 0 到 1 搭建项目的底层骨架，跑通一个能用来“出题、组卷、看倒计时做题”的最基础闭环版本。

- **🛠️ 核心技术栈敲定**：
  - **后端**：选用 `Python 3.12` + `FastAPI` (极速 API 框架) + `MySQL 8.0`，搭配 `SQLAlchemy` ORM 管理数据库。
  - **前端**：选用 `Vue 3` + `Vite` (极速构建) + `TypeScript` (严格类型检查)，保证代码不出低级错误。
- **🎨 UI 与页面设计**：
  - B端大量采用了“毛玻璃 (Aero Glass)”质感和沉浸式布局，力求界面充满极简的高级感。
  - 跑通了：题库的增删改查、Excel 批量导入、可视化的拖拽组卷。
- **🧪 自动化测试把关**：
  - 后端一开始就引入了 `Pytest` 测试框架，写了几十个测试用例来模拟用户登录、交卷、并发请求等。不仅在内存数据库里跑，还在真实的 MySQL 库里进行了端到端回归测试，确保逻辑 100% 正确。
- **📑 奠基性的四大文档**：在动手写哪怕一行代码之前，我们先输出了四个极度详尽的核心文档：
  1. `product.md`：产品需求说明书 (PRD)，明确了我们要做什么。
  2. `tech-spec.md`：技术架构规范，约定了后端的代码怎么分层。
  3. `api-contract.md`：接口契约，让前端和后端可以看着同一份文档并行开发。
  4. `agent.md`：专门写给 AI 助手的行为准则（比如怎么写变更日志、绝不能造假版本号等）。
