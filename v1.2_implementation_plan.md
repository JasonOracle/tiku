# 智题库 (TiKu) v1.2 产品需求与架构方案 (Draft)

## 1. 目标与背景
基于 v1.0 MVP，v1.2 将全面贯彻**“AI 平权”与“五可架构”**理念，引入权限管理、主观题型，并将 AI 作为真实的“数字员工”融入业务流（阅卷与出题），打造真正的 AI-Native 测评系统。

## 2. 核心架构决议 (已对齐)

### 2.1 权限管理 (RBAC) 
- **角色架构**：超级管理员 (Super Admin)、普通管理员 (Normal Admin, 老师)、AI员工 (AI Employee)。
- **数据可见性 (方案二)**：
  - **题海池共享**：所有老师共享题库资源。
  - **试卷隔离**：老师只能管理和批改自己创建的试卷。
- **题目锁定防篡改 (方案 A)**：一旦题目被处于“已上架”或“已归档”的试卷引用，该题目将进入“全局只读”状态，不可修改（若需修改只能复制产生新题），杜绝历史数据和正在进行的考试发生崩塌。

### 2.2 题型扩展与判定规则
- **填空题 (客观判卷)**：
  - 支持“一空多答”（如配置 ["北京", "北京市"] 均算对）。
  - **无需 AI 阅卷**：系统底层采用传统代码执行 100% 强一致性匹配。在对比前，系统会自动切除学生答案的首尾空格并统一转化为大写（忽略大小写），只要命中设定答案即给分，瞬间出成绩。
- **简答题 (主观判卷)**：
  - 教师端录入题目、标准答案和“踩分点”。
  - 学生端为多行文本输入。交卷后进入“待批改”状态，不立即出分。

### 2.3 AI 阅卷 (人机协同大厅与全托管模式)
- **试卷级全托管配置 (Auto-Approve)**：在 B 端**创建试卷/编辑试卷的弹窗**中，新增开关 `[是否开启 AI 全权阅卷]`（底层对应字段 `is_ai_auto_grade`）。
  - **动态显示逻辑 (UI 优化)**：该开关仅在试卷中**包含简答题时才显示**。如果试卷全是客观题或填空题，该开关隐藏，防止误导老师。
  - **若开启 (全权托管)**：该试卷交卷后，AI 会自动批阅简答题，批阅完毕直接发布总成绩，无需老师复核。  - **若关闭 (预批改模式)**：AI 仅生成建议分数和评语，试卷进入“待复核”状态，必须由老师在阅卷大厅逐一确认后才发布成绩。
- **阅卷大厅 UI**：展示题目及踩分点、学生原文、AI 判卷结果卡片。支持一键“采信全部 AI 评分”。
## 3. 新增需求规划：AI 自动出题与组卷 (AI Generator)

> [!TIP]
> **关于 AI 出题与组卷的架构思考 (基于 AI 平权)**
> 让 AI 参与内容的创造是非常契合我们系统理念的，这赋予了 AI 员工“内容建设者”的身份。

### 3.1 AI 生成题目 (Question Generation)
- **交互方式**：在 B端“题海管理”页面增加【✨ AI 出题】按钮。老师提供一段材料文本，或简单的自然语言描述（例如：“生成5道关于Python并发编程的单选题，带详细解析，难度中等”）。
- **底层机制**：AI 生成结构化的 JSON，然后**以 AI 员工的身份**去调用系统标准的“新增题目 API”。
- **风险控制**：生成的题目全部带有“AI生成”的专属来源标签，强制要求老师进行“入库前二次确认”，防止幻觉导致的题目错漏。

### 3.2 AI 智能组卷 (Exam Generation)
- **交互方式**：在“试卷管理”页面增加【✨ AI 智能一键组卷】功能。老师输入类似：“帮我组一份50分的消防安全测试卷，要求3道单选，2道判断，1道简答”。
- **底层机制**：
  1. AI 优先从**当前共享题库**中检索符合条件的题目。
  2. 若题库数量不足，AI 自动生成新题补齐。
  3. AI 调用“创建试卷 API”，生成试卷记录。
- **风险控制**：试卷生成后，系统强制将其置为 **Draft (草稿)** 状态。必须由人类老师点开检查无误后，手动点击“上架”方可让学生作答。杜绝未审核的劣质内容直接流向前端。

## 4. 全新交互探索：消息中心与 AI Copilot (Copilot Workspace)

> [!TIP]
> **探索“事件式交互”**
> 突破传统的“找菜单”模式，引入对话即服务的理念，让 AI 员工成为每个老师的专属数据助理。

### 4.1 系统消息与通知流 (Notification Center)
- **站内信机制**：B 端增加“消息中心”模块。
- **场景联动**：
  - 当 AI 员工完成某份试卷的批量预批改后，主动给阅卷老师发送通知：“您有 35 份《期中考试卷》已被 AI 预批改完毕，请前往阅卷大厅复核。”
  - 当 AI 自动生成一套新试卷草稿后，发送通知提醒去查收。

### 4.2 B 端 AI 助手专属 Tab (Copilot / AI 工作台)
- **交互形态**：在 B 端左侧菜单栏增加一个独立的 **[✨ AI 助手]** Tab，或者在全局右侧悬浮一个对话抽屉。
- **核心能力 (基于可读、可分析架构)**：
  - **角色与权限认知**：老师可以问“我是谁，我能干什么？” AI 会读取当前 Token，回答“您是普通教师，您可以管理自己的试卷，也可以去公共题库挑题，但我发现您目前还没有创建过任何分类哦。”
  - **业务数据问答 (Data Chat)**：老师可以用自然语言查询业务报表。例如输入：“帮我总结一下张老师最近一周出了多少道题，组了多少份卷子，学生的平均正确率是多少？”。AI 会在后端调用统计 API，聚合数据后以图表或文本回答。
  - **操作意图路由**：如果老师输入“帮我出一份题”，AI 会通过 Function Calling 自动唤起出题组件。
- **权限安全**：AI 助手的回答范围严格受到提问者真实角色权限的限制（比如普通老师不能通过助手查到系统后台的敏感密钥）。

### 4.3 双域留痕与高危阻断 (Audit & Risk Management)
这一条是落实“可约束”底线的关键红线：
- **双日志全量留痕**：无论后端接口是被前端的人类管理员调用，还是被 AI 员工调用，只要发生“写操作（增删改）”，系统必须在审计日志表（`audit_logs`）中完整记录：`操作时间`、`操作人ID`（包含AI专属ID）、`动作类型`、`操作前数据快照`、`操作后数据`。
- **高低危动作分级与审批**：
  - **低风险自动执行**：如 AI 读取统计数据、AI 自动批改“客观/填空题”、AI 在 Copilot 中回答咨询等，可以直接放行。
  - **高风险强制确认**：如 AI 智能组卷后发布、AI 预批改简答题、AI 发现异常分数试图重置试卷状态等。此类操作 AI 只能产生“执行意图表单”或者“草稿”，强制推送到人类员工的「消息中心」或界面进行显式的 `[人工确认]` 后，方可实际写入主业务表。杜绝任何 AI 绕过人眼审查去直接篡改敏感数据的可能性。

## 5. MVP 落地妥协与边界 (MVP Pragmatism)
既然是 v1.2 的 MVP 版本，我们需要防止过度工程，确保核心理念能快速跑通落地。我补充了以下 3 点关键考量：

### 5.1 C 端用户体验的闭环补齐 (UI States)
引入简答题后，C 端考生的体验会发生变化：
- **“待批阅”状态**：学生交卷后不能再直接弹出分数。历史答题记录列表中必须新增 `[批阅中]` 状态标签。
- **降级查看**：在成绩正式出炉前，学生点击记录只能查看自己的原答案，无法查看标准答案（防泄题），待老师（或AI复核后）发布成绩，方可查看完整解析报告。

### 5.2 极简的 AI 后台调度 (Lightweight Architecture)
- **避免过重的基础设施**：作为轻量级平台，不要为了 AI 阅卷引入 Celery + Redis 这种重型消息队列。
- **FastAPI 协程并发**：我们可以直接利用 FastAPI 原生的 `BackgroundTasks` 来做轻量级的异步阅卷。学生交卷瞬间立刻返回 200，随后 FastAPI 后台自动请求大模型。如果失败了，允许老师在 B 端点击【重新触发 AI 批改】作为兜底机制。

### 5.3 AI 算力资产化与个人额度管控 (User Quota)
仅仅做粗暴的频率限制还不够商业化，我们需要将 AI 能力“资产化”：
- **超级管理员看板**：在 B 端后台新增【AI 算力管理】模块（仅 Super Admin 可见）。超级管理员可以为每个普通教师单独分配“每日 Token 额度（或每日调用次数）”。
- **个人额度感知**：普通教师在自己的 B 端界面（如右上角头像旁，或 AI Copilot 助手面板中），可以直观地看到自己的 AI 余额（例如：🔋 剩余可用 AI 额度：15 次）。每次让 AI 出题或组卷前，必须校验余额，余额不足则拦截并提示联系管理员充值。
- **技术落地**：后端在 `admins` 表增加 `daily_ai_quota` 字段，配合记录流水以实现按天统计。

### 5.4 【MVP 降级商榷】系统级 AI 网关与高可用管理
*原计划：在超管后台提供多模型 UI 配置与底层自动降级 (Fallback) 机制。*
**MVP 瘦身建议**：**建议砍掉后台 UI 配置与降级逻辑**。
对于 MVP 来说，做一套 AI 网关太重了。建议改为：直接在后端的 `.env` 配置文件中写死一个主力模型（比如 DeepSeek-V3）的 Key 和 URL 即可。等验证了产品商业模式（v2.0），再去开发多模型自动 Fallback 机制。超管看板依然保留，只需统计算力消耗即可。

## 6. 核心逻辑排雷与 C 端体验重构 (Fixes & UX)

### 6.1 致命逻辑漏洞修复：AI 额度内外有别
必须对 AI 算力的消耗做**场景隔离**：
- **主动创造型（扣个人额度）**：老师主动点击“AI 出题”、“AI 组卷”、“AI 助手聊天”，严格扣减个人 `daily_ai_quota`。
- **被动业务型（扣系统大盘）**：学生交卷触发的“AI 预批改/自动阅卷”，是系统的核心业务职责，消耗不算在老师个人头上，而是记录在系统级账单中。避免大班级老师因学生交卷太多导致自己没额度工作。

### 6.2 技术规约：客观题规则与底层数据完整性 (Data Integrity)
- **多选题半对机制**：增加配置项——多选题“漏选得一半分，错选不得分”。
- **填空题多空校验**：在出题保存入库时，后端强制校验 `题干中 ___ 的数量` == `answer 二维数组的长度`。
- **公共题库的“防牵连”软删除 (Soft Delete)**：在“共享题库”模式下，老师 A 创建的题可能被老师 B 加到了草稿卷子里。如果老师 A 此时物理删除该题，会导致老师 B 的试卷直接崩溃（外键断裂）。**修复方案**：题库中的题目绝对不允许物理删除，只能打上 `is_deleted = 1` 的软删除标记，前端题库不再展示，但已引用的试卷依然能正常拉取到原题。

### 6.3 阅卷底层逻辑漏洞修复：原子化 Prompt 与动态满分
- **动态满分问题**：同一道简答题（原始默认 10 分），被组卷时老师可能把分值改成了 20 分。AI 在阅卷时，必须传入 `exam_questions.score`（20分）而不是原题分，否则给分比例会全错。
- **原子化防局部失败**：如果一张试卷有 5 道简答题，千万不能让后端去请求 5 次大模型（极易出现某几道题成功、某几道题超时失败的局部残缺状态，且严重浪费 Token）。必须将 5 道题的作答合并拼装成一个大 JSON Prompt，发起**单次请求**，要么全部批改成功，要么全部失败进入异常兜底，保证数据一致性（原子性）。

### 6.4 C 端“我的测试”全状态流转架构 (UX 升级)
底部的 TabBar 增加【我的测试】。配合试卷的 `start_time` 与 `end_time`，打造极其清晰的顶部 Tabs 过滤：
1. **进行中**：当前时间在试卷允许作答区间内，可点击参与的考试。
   - **时间边界与僵尸考卷漏洞修复 (Zombie Exams)**：
     - **时间倒挂**：距离 `end_time` 仅剩 10 分钟时，倒计时强制压缩为 10 分钟。
     - **逃逸异常 (MVP 懒计算降级)**：如果学生未交卷直接关掉浏览器，试卷会卡在 `in_progress`。
     *原计划：用 Redis 或定时任务扫表强制收卷（太重）。*
     **MVP 瘦身建议**：**采用“懒惰求值（Lazy Evaluation）”**。不需要任何后台定时任务。当这个学生下次打开“我的测试”列表，或者老师打开“阅卷大厅”去拉取数据时，后端代码在这时“顺手”检查一下有没有过期的卷子，如果有，当场把它们强制转为 `已交卷`。0 运维成本，完美符合 MVP。
2. **未开始**：当前时间还没到开考时间的试卷。卡片底部按钮呈现 `Disable (禁用)` 状态，提示“考试还未开始”。
3. **已考试**：
   - **多记录聚合**：按试卷聚合展示，卡片上的状态永远反映**最新一次作答**的成绩。
   - **防作弊漏洞修复（解析泄露倒挂）**：如果考试结束时间是中午 12:00，一个学生 10:30 就交卷且及格了，他绝不能立刻点击“查看解析”，否则他可以把答案发给还在考的同学。**修复方案**：`查看解析` 按钮必须在 `当前时间 > 试卷 end_time` 后才能高亮点击。未到时间前，点击提示“考试尚未结束，解析暂未开放”。

### 6.5 联动的 B 端全景大盘 (B-end UX Alignment)
既然 C 端引入了时间窗口和审批状态，B 端的管理视图必须完全对齐：
1. **组卷弹窗加配“时间锁”与强制校验**：
   - 在表单中强制加入 `[考试开始时间 start_time - 结束时间 end_time]` 的日期选择器。
   - **时间悖论强制校验**：必须满足 `考试时长 (time_limit)` ≤ `(end_time - start_time)`。如果老师设置区间只有 3 分钟，却要求考 20 分钟；或者区间是 60 分钟，要求考 61 分钟，**前端表单直接标红报错禁止提交**，提示：“考试限时不能大于考试开放总区间时间”。
2. **试卷列表的双维状态展示**：
   - **主状态 (生命周期)**：`草稿 (Draft)`、`已上架 (Published)`、`已归档 (Archived)`。
   - **子状态 (时间流逝指示，仅“已上架”状态下显示)**：动态打上 `[未开始]`、`[进行中]`、`[已结束]` 的彩色 Badge。
3. **待办红点与全托管宕机兜底 (Action Alert & Fallback)**：
   - 正常待办：试卷列表对应行高亮显示 `[N份待批阅]`，点击进入阅卷大厅。
   - **全托管宕机漏洞修复**：如果老师在组卷时开启了 `[AI 全权阅卷]`，但碰巧那天大模型 API 宕机或额度超限，学生的卷子绝不能卡死在“批阅中”。系统会自动把这些失败的卷子降级弹回 B 端试卷列表，亮起 `[异常:N份待批阅]` 红点。老师点进去后，可以手动批改，或者点击【重新触发 AI】。

### 6.6 【精益实现】B 端 AI Copilot 的“失忆症”防范 (Context Pre-fetching)
*原计划：后端做复杂的 SQL 分析和动态上下文查询。*
**MVP 轻量化重构（借鉴 Cursor/Vercel AI 经验）**：
保留“AI 知道我在干嘛”的神奇体验，但**彻底抛弃后端 RAG 或 Text-to-SQL 的极高开发成本**。采用 **“前端状态快照注入 (Frontend State Preamble)”** 模式：
1. 老师在操作 B 端系统时，前端 Pinia/Vuex 本来就存有大量数据（如：当前登录老师叫什么、当前停留在哪个页面、仪表盘接口返回的待阅卷数量：12、剩余额度：15 等）。
2. 当老师在聊天框发起提问（如“总结下我今天的任务”）时，前端在发送请求前，悄悄把这些已有的**全局状态打包成一段极简的 Markdown 文本**，硬拼接在 User Prompt 的最前面发给后端。
   *（例如：`[系统隐藏上下文：当前用户=王老师，待批改试卷=12份，剩余额度=20次] \n\n 老师提问：我今天该干嘛？`）*
3. 这样，大模型瞬间“拥有了记忆”和“上帝视角”，能给出极其精准的回答。开发成本几乎为 0，因为只是前端拼字符串而已，但用户体验拉满！

### 6.7 【MVP 降级商榷】惊艳 C 端的微创新：✨ AI 错题助教 (AI Tutor)
*原计划：在学生错题旁增加 AI 实时讲解功能。*
**MVP 瘦身建议**：**果断砍掉**。
这是一个属于“锦上添花”的功能。MVP 的核心闭环是“老师出卷 -> 学生考 -> AI/老师改”。AI 错题助教不仅会大幅增加大模型的 Token 支出，还会增加 C 端的 UI 复杂度和接口开发量。完全可以留作下个版本的重要卖点。
