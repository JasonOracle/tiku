# 智题库 (TiKu) v1.2 保姆级开发实施指南 (面向外部 AI 开发者)

> **⚠️ 致接手本项目的 AI 开发者（重要必读）**：
> 1. **严禁自由发挥**：严格按照本指南规划的 Step 1 到 Step 6 逐步编写代码，严禁自行引入 Celery、Redis、LangChain 等重型框架。
> 2. **严禁破坏现有逻辑**：本系统 v1.1 已全量验证通过，只能在现有框架上**做增量扩展**，绝不能破坏原有的 JWT 鉴权、数据治理删除拦截等已有逻辑。
> 3. **全员代码规范**：所有新增/修改的文件，必须在顶部添加标准变更日志；新增的 Vue 组件同级目录下必须配备 `.md` 说明文档。

---

## 目录索引
- [0. 绝对禁止清单 (哪些千万不能干)](#0-绝对禁止清单-哪些千万不能干)
- [1. 阶段一：数据库与多级 RBAC 账号数据种子 (Step 1)](#1-阶段一数据库与多级-rbac-账号数据种子-step-1)
- [2. 阶段二：试卷 grading_mode 与主观题算分引擎改造 (Step 2)](#2-阶段二试卷-grading_mode-与主观题算分引擎改造-step-2)
- [3. 阶段三：试卷管理行内待办与批阅抽屉 GradingDrawer (Step 3)](#3-阶段三试卷管理行内待办与批阅抽屉-gradingdrawer-step-3)
- [4. 阶段四：组卷体验升级（普通组卷透亮选项 + AI组卷极简化）(Step 4)](#4-阶段四组卷体验升级普通组卷透亮选项--ai组卷极简化-step-4)
- [5. 阶段五：AI 助管 Copilot 现代交互与 Action Card 协议 (Step 5)](#5-阶段五ai-助管-copilot-现代交互与-action-card-协议-step-5)
- [6. 阶段六：自动化测试回归与端到端验证 (Step 6)](#6-阶段六自动化测试回归与端到端验证-step-6)

---

## 0. 绝对禁止清单 (哪些千万不能干)

在动手写任何代码前，AI 必须牢记以下底线：
1. ❌ **禁止私自安装新的第三方重型包**：
   - 严禁引入 `celery`, `redis`, `langchain`, `llama-index`, `ofetch`, `moment`。
   - 异步任务一律使用 FastAPI 原生的 `BackgroundTasks`。
   - 大模型调用一律使用标准 `httpx` 或 Python 原生 `requests`。
2. ❌ **禁止在界面露出底层模型供应商商业名称**：
   - 界面上严禁出现 `SenseNova`、`商汤`、`Agnes`、`DeepSeek`。
   - 统一使用中性专有名词：**「AI 智能助管」** 或 **「AI 智算引擎」**。
3. ❌ **禁止为出题人开放他人试卷或全站宏观看板**：
   - 查询试卷列表时，出题人（`role == 'creator'`）强制追加 SQL 过滤条件：`.filter(Exam.creator_id == current_user.id)`。
4. ❌ **禁止管理员向同级或上级超管充值额度**：
   - 划拨额度接口中必须强校验：`target_user.role == 'creator'` 且 `target_user.created_by_id == current_admin.id`。违背则直接返回 `403`。
5. ❌ **禁止任何角色物理删除已被引用的题目和含有考试记录的试卷**：
   - 题目删除必须走 `is_deleted = 1` 软删除。
   - 试卷有作答记录一律报错拦截，引导用户下架归档。

---

## 1. 阶段一：数据库与多级 RBAC 账号数据种子 (Step 1)

### 1.1 修改 ORM 模型文件
#### 目标文件：`backend/app/models/user.py`
- 在 `Admin` 模型中新增字段：
  ```python
  role = Column(String(20), default="admin", nullable=False) # 'super_admin' | 'admin' | 'creator'
  created_by_id = Column(Integer, ForeignKey("admins.id", ondelete="SET NULL"), nullable=True) # 记录由谁创建
  daily_ai_quota = Column(Integer, default=100, nullable=False) # 每日主动 AI 额度
  ```

#### 目标文件：`backend/app/models/question.py`
- 在 `Question` 模型中新增字段：
  ```python
  is_deleted = Column(Boolean, default=False, nullable=False) # 软删除标记
  ```

#### 目标文件：`backend/app/models/exam.py`
- 在 `Exam` 模型中新增字段：
  ```python
  grading_mode = Column(String(20), default="manual", nullable=False) # 'manual' | 'ai_pre' | 'ai_auto'
  start_time = Column(DateTime, nullable=True)
  end_time = Column(DateTime, nullable=True)
  creator_id = Column(Integer, ForeignKey("admins.id", ondelete="CASCADE"), nullable=True)
  ```

#### 目标文件：`backend/app/models/record.py`
- 在 `ExamRecord` 模型中：
  - `status` 枚举扩展支持：`'pending_grading'`（待批阅）。
  - 新增字段 `ai_grading_result = Column(JSON, nullable=True)` 存储 AI 的初评分与各题评语。

#### 目标文件：`backend/app/main.py`
- 在 `auto_patch_db_columns()` 启动自动补列函数中，为上述新增字段添加 `ALTER TABLE ... ADD COLUMN IF NOT EXISTS` 原生 SQL 补丁，确保服务启动时自动平滑升级数据库物理列。

### 1.2 编写种子脚本创建 6 个测试账号
#### 目标文件：`backend/seed_rbac_users.py` (新建)
- 编写一个可独立运行的 Python 脚本，创建以下层级账号（统一密码 `123456`，使用 `backend/app/core/security.py` 的 `get_password_hash` 进行 Hash 处理）：
  1. `admin@tiku.io`（超管，`super_admin`，初始额度 99999999）
  2. `manager.zhang@tiku.io`（管理员A，`admin`，`created_by_id = admin.id`，初始额度 1000）
     - 绑定下级出题人 A1：`creator.chen@tiku.io`（出题人，`creator`，`created_by_id = manager.zhang.id`，初始额度 100）
     - 绑定下级出题人 A2：`creator.lin@tiku.io`（出题人，`creator`，`created_by_id = manager.zhang.id`，初始额度 100）
  3. `manager.liu@tiku.io`（管理员B，`admin`，`created_by_id = admin.id`，初始额度 1000）
     - 绑定下级出题人 B1：`creator.wang@tiku.io`（出题人，`creator`，`created_by_id = manager.liu.id`，初始额度 100）
     - 绑定下级出题人 B2：`creator.zhao@tiku.io`（出题人，`creator`，`created_by_id = manager.liu.id`，初始额度 100）

---

## 2. 阶段二：试卷 grading_mode 与主观题算分引擎改造 (Step 2)

### 2.1 试卷创建与时间锁校验
#### 目标文件：`backend/app/api/admin/admin_exams.py`
- **时间悖论硬校验**：
  在 `create_exam` 与 `update_exam` 中校验：
  ```python
  if exam_in.start_time and exam_in.end_time:
      delta_minutes = (exam_in.end_time - exam_in.start_time).total_seconds() / 60
      if exam_in.time_limit and exam_in.time_limit > delta_minutes:
          raise HTTPException(status_code=400, detail="考试限时不能大于开放总区间时间")
  ```
- **出题人归属写入**：
  创建试卷时强制写入 `exam.creator_id = current_admin.id`。
- **出题人查询隔离**：
  查询试卷列表时：
  ```python
  if current_admin.role == 'creator':
      query = query.filter(Exam.creator_id == current_admin.id)
  ```

### 2.2 交卷判分流转逻辑改造
#### 目标文件：`backend/app/services/exam_service.py` 中的 `submit_exam_record_with_lock`
- 检查该试卷所包含的题目类型：
  - **若全为客观题/填空题**：执行原生算分，直接置为 `status = 'submitted'`，立出总成绩。
  - **若包含简答题 (`type == 'short'`)**：
    - 先把客观题的分数算出来，暂存为客观题小计。
    - **分支 1（`grading_mode == 'ai_auto'`）**：
      调用后台异步批阅任务，大模型算完简答题后与客观题相加，写库更新总分并置为 `status = 'submitted'`。
    - **分支 2（`grading_mode == 'ai_pre'` 或 `'manual'`）**：
      记录状态置为 `status = 'pending_grading'`。
      若为 `ai_pre`，后台异步请求大模型生成初评建议分存入 `ai_grading_result`；若为 `manual` 则直接等待人工批阅。

---

## 3. 阶段三：试卷管理行内待办与批阅抽屉 GradingDrawer (Step 3)

### 3.1 试卷列表行内待办微章
#### 目标文件：`tob/src/views/exams/ExamsView.vue`
- 在试卷列表的“试卷标题”或“操作”列中，动态统计该试卷名下处于 `pending_grading` 状态的答卷数量。
- 若 `pending_count > 0`，在行内渲染醒目的橙色胶囊微章：
  ```html
  <el-tag type="warning" effect="dark" class="cursor-pointer" @click="handleOpenGrading(row)">
    {{ row.pending_count }} 份待批阅
  </el-tag>
  ```

### 3.2 实现批阅抽屉组件
#### 目标文件：`tob/src/views/exams/components/GradingDrawer.vue` (新建)
- **UI 布局**：
  - 顶部：显示当前试卷名称、待批卷进度（如：`第 1 / 3 份`）。
  - 中间：展示考生的简答题原文作答、题目标准答案及采分点。
  - **AI 建议展示区**：
    若有 `ai_grading_result`，展示浅蓝框提示：`🤖 AI 初评建议分：8 分 (满分 10分) | 理由：回答切中要点`。
  - **打分输入框**：预填 AI 建议分，允许老师手动滑动或输入微调。
  - 底部操作栏：主色大按钮 `[✔ 确认发布成绩]`，支持一键切换下一份。
- **同级文档**：创建同名 `GradingDrawer.md` 说明设计初衷与调用示例。

---

## 4. 阶段四：组卷体验升级（普通组卷透亮选项 + AI组卷极简化）(Step 4)

### 4.1 普通组卷弹窗：题目透亮展示（选项与正确答案高亮）
#### 目标文件：`tob/src/views/exams/ExamsView.vue`
- 改造底部“试卷已加入题目”表格：
  - 题干列下方展开渲染选项列表：
    - 单选/多选：普通选项灰色小标签；**正确答案选项以绿色背景、浅绿边框、绿色加粗文字并前置 `✔` 渲染**。
    - 判断题：挂出绿色 `[✔ 正确]` 或红色 `[✖ 错误]` 标签。
    - 填空/简答题：展示紧凑的标准答案/采分要点。
  - 操作列：**严格只保留 `▲ / ▼` 上下移动排序与 `🗑️` 移出，坚决不展示快速编辑按钮**。
- **简答题智能展开**：
  - 监听已选题目列表，若全为客观题，隐藏 AI 阅卷选项。
  - 检测到至少 1 道简答题时，动态展开 `grading_mode` 单选组（人工全权 / AI辅助预审 / AI自动托管）。

### 4.2 AI 智能一键组卷弹窗极简化
#### 目标文件：`tob/src/views/exams/components/AiExamDialog.vue` (或对应组卷弹窗)
- **表单布局瘦身**：
  - 主界面只保留两项必填视觉：
    1. `* 组卷需求` (Prompt 文本域)
    2. `* 考试时间窗口` (起止时间选择器)
  - 隐藏高级选项：使用 `<el-collapse>` 折叠面板将“试卷标题、试卷分类、难度、及格比例”折叠收起，赋予默认值。
- **生成后所见即所得审阅清单**：
  - 点击“开始 AI 组卷”后，大模型实时全量原创生成题目。
  - 弹窗原地切换为“题目审阅卡片”，同样复用上述的“选项与正确答案绿色高亮”排版。
  - 底部保留主按钮 `[确认保存试卷]`，由出题人/管理员确认无误后最终点击落库。

---

## 5. 阶段五：AI 助管 Copilot 现代交互与 Action Card 协议 (Step 5)

### 5.1 视觉风格改版与脱敏
#### 目标文件：`tob/src/views/ai/AiAssistantView.vue` (或对话组件)
- **厂商名称脱敏**：全站文本统一替换为 **「AI 智能助管」** 或 **「AI 智算引擎」**。
- **现代消息气泡样式**：
  - AI 消息外层容器：柔和浅灰底（`#f8fafc`），细边框（`1px solid #e2e8f0`），圆角 `12px`。
  - 顶部添加引用条：`| 回复 全Ai系统: [用户上一条问题摘要]`。

### 5.2 实现两种交互卡片 (Action Cards)
1. **人机协同待确认卡片 (`action_card`)**：
   - 针对敏感操作（如管理员向出题人划拨额度）。
   - 解析大模型输出中的 ````action_card ... ```` 代码块，渲染为红调警示卡片：
     - 红色 Header：`确认: xxx 业务操作 [待确认]`
     - 操作明细与风险等级。
     - 底部按钮组：红色胶囊按钮 `[确认执行]` 与次按钮 `[取消]`。
     - 点击确认后调真实接口，卡片原地流转为绿色 `已执行`。
2. **交互式操作路由卡片 (`action_list`) 与权限白名单**：
   - 针对出题人问待办（如“我有几套题没批？”）。
   - 解析大模型输出的 ````action_list ... ````，渲染为任务列表卡片：
     - 每一行展示试卷名、待批数量，右侧操作条带 `[去批改]` 按钮。
     - **权限守卫**：
       - 前端状态快照注入时，出题人仅注入其私有试卷数据。
       - 前端维护 `ACTION_WHITELIST` 动作白名单，出题人无权触发人员/额度等管理动作。
       - 点击 `[去批改]` 直接原地唤出对应试卷的批阅抽屉（`GradingDrawer`）。

---

## 6. 阶段六：自动化测试回归与端到端验证 (Step 6)

修改完成后，必须执行以下三项命令并确保 100% 通过：
```bash
# 1. 运行后端全量自动化测试
cd backend
python -m pytest tests/ -v

# 2. 运行 B 端前端类型检查与构建
cd ../tob
pnpm build

# 3. 运行 C 端前端类型检查与构建
cd ../toc
pnpm build
```

---

> **结语**：只要严格按照上述步骤逐步执行，不跳步、不自作主张新增抽象、不破坏原有数据治理矩阵，任何 AI 开发者都能极其顺畅、高质量地交付 TiKu v1.2！
