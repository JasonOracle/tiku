# 智题库 (TiKu) v1.2 产品需求文档 (PRD)

## 1. 目标与背景
基于 v1.1 MVP，v1.2 将全面贯彻**“AI 平权”与“五可架构”**理念，引入企业级多级权限体系、主观简答题判分引擎、AI 全权阅卷与预审机制、现代卡片化 AI 助管（Copilot）以及极简化 AI 组卷，打造可落地的开源工业级 AI-Native 测评系统。

---

## 2. 核心架构决议与 B 端三级角色模型 (RBAC Hierarchy)

### 2.1 B 端三级权限分工（金字塔向下兼容）
B 端管理后台彻底脱敏“教师”叫法，确立严谨的三级管理权限：
1. **超级管理员 (Super Admin)**：
   - **最高管理权**：全域人员账号创建与管理、全域 AI 算力配额调配划拨、系统审计日志 (`audit_logs`) 审查。
   - **向下兼容业务权**：具备管理员与出题人的全部业务能力（可自由在共享题海出题、可在线组卷并统管全站试卷生命周期）。
   - **红线限制**：不可删除系统最后唯一的超管账号；不可违背全员通用数据治理铁律。
2. **管理员 (Admin)**：
   - **中层统管权**：统筹全站试卷生命周期（发布/下架/归档）、阅卷大厅成绩统筹、**向下级（出题人）划拨与充值 AI 额度**。
   - **向下兼容业务权**：具备出题人的全部业务能力（可在共享题海出题、可在线组卷）。
   - **红线限制**：**严禁向同级管理员或上级超管充值/改动额度**；无权修改人员角色等级与超管账号；无权清空或删除审计日志。
3. **出题人 (Creator / 基础业务单元)**：
   - **基础出题权**：全权维护公共题海（创建题目、编辑题目、查看题目、软删除题目）。
   - **私有组卷权**：允许在线组卷，但实行**严格的私有化隔离**（出题人只能看到和管理自己创建的试卷，无权查看他人的私有试卷与全站宏观看板）。
   - **红线限制**：无权管理人员与分类；无权调配任何人的 AI 额度（个人额度耗尽后向上级申请）；无权查看系统审计日志。
4. **层级溯源与审计绑定**：
   - 所有创建的账号必须记录 `created_by_id`，全程可追溯其上级归属（如出题人由哪位管理员注册）。

### 2.2 🚫 全员通用数据治理铁律 (所有角色绝对遵守)
无论超管、管理员还是出题人，底层系统对以下行为一票否决：
1. **禁止反转归档试卷**：`archived`（已归档）为彻底不可逆终态，任何人不能将其重新上架。
2. **禁止物理删除有作答记录的试卷**：只要试卷存在考生的有效提交记录（`submitted`/`timeout`），任何人不得物理硬删除，保护证据链。
3. **禁止创建时间倒挂的悖论试卷**：组卷时强制校验 `考试时长 (time_limit) ≤ (结束时间 end_time - 开始时间 start_time)`，违背则前后端一律报错拦截。
4. **禁止硬删除已被引用的题目**：题目一旦被任何试卷引用，任何人不得物理删除，系统强制执行软删除（`is_deleted = 1`），前端题海隐藏，试卷历史引用不崩溃。
5. **写操作无条件审计留痕**：任何角色对核心数据（改分、调权限、划拨额度、删题、改卷）的写操作，系统全量写入 `audit_logs`。

---

## 3. 题型判定与简答题 AI 辅助阅卷

### 3.1 客观题与填空题引擎 (毫秒级纯本地判分)
- **单选/判断题**：系统本地 100% 强一致性比对，交卷瞬间秒出分，**不触发大模型**。
- **多选题半对机制**：漏选得 50% 分值，错选得 0 分。
- **填空题防崩溃强比对**：
  - 题干统一使用三个下划线 `___` 占位，答案为二维数组（支持一空多答，如 `["北京", "北京市"]`）。
  - 保存时强校验 `题干 ___ 数量 == answer 数组长度`。
  - 判题时自动切除首尾空格并转大写强比对，**不触发大模型**。

### 3.2 简答题 (主观题) 与试卷级 AI 阅卷模式
大模型**仅在试卷包含主观简答题时触发**。AI 阅卷模式配置在**试卷维度 (`grading_mode`)**，不在单个题目上配置：

- **动态感知展示**：
  - 若试卷全为客观题/填空题：AI 阅卷选项直接在界面隐藏（纯代码秒出分）。
  - **若检测到包含至少 1 道简答题**：试卷编辑弹窗动态展开模式选择：
    1. **人工全权批阅 (`manual`)**：不消耗 AI 额度，交卷后进入 `pending_grading`，出题人/管理员手动批改打分。
    2. **AI 辅助预审 (`ai_pre`)**：学生交卷后，后台异步调大模型生成初评建议分与采分点评语，答卷进入待复核状态，**等待出题人/管理员确认发布**。
    3. **AI 自动托管 (`ai_auto`)**：学生交卷后，后台大模型异步自动打分，并与客观题合并计算总分，**直接出成绩并发布**。
- **现代交互：试卷行内下钻批阅 (废除独立阅卷大厅大菜单)**：
  - 摒弃老旧的大系统独立“阅卷大厅”一级菜单，全面拥抱轻量化设计：
  - 在【试卷管理】列表中，若某试卷存在待批改答卷，行内醒目高亮呈现橙色微章：**`[N 份待批阅]`**。
  - 老师点击该徽章，直接原地滑出**「主观题批阅抽屉」**，看到考生简答题作答、标准答案采分点及 AI 初评建议分，老师一键点击 `[确认发布成绩]`，批完徽章自动递减/消失。
- **原子化 Prompt 与动态满分**：
  - 一张试卷若有多个简答题，后端必须组装为**单次大 JSON Prompt** 发起一次性调用，杜绝局部成功局部失败。
  - 传入当前试卷分配的实际分值 (`exam_questions.score`) 算分，杜绝原题默认分漂移。
- **全托管宕机降级兜底**：
  - 若大模型调用超时或报错，系统自动将答卷回退降级为人工待批阅状态（`pending_grading`），并在试卷列表对应行亮起 `[异常:N份待批阅]` 警示，支持人工接管或一键重试。

---

## 4. 组卷与题目列表展示体验双重优化

### 4.1 普通新建/编辑试卷弹窗 (`ExamsView.vue`)
- **题目选项与正确答案透亮展示**：
  - 底部“试卷已加入题目”列表中，题干下方紧凑展示该题的选项明细。
  - **单选/多选**：普通选项为浅灰字标签；**正确答案选项以浅绿底 + 绿色边框 + 绿字加粗 + 勾勾图标显式突出（`✔ [A] 答案`）**。
  - **判断题**：显式挂出 `[✔ 正确]`（绿）或 `[✖ 错误]`（红）Badge。
  - **填空/简答**：紧凑展示标准答案或采分点。
  - **操作列极净化**：保留 `▲ / ▼` 上下移动排序与 `🗑️` 移出，**不展示快速编辑**，防止弹窗嵌套过度工程。

### 4.2 AI 智能一键组卷弹窗
- **主视觉极简化（仅两项硬必填）**：
  1. `* 组卷需求` (Prompt 文本域)：输入出题科目、数量、题型、知识点要求。
  2. `* 考试时间窗口`：开始时间至结束时间（必填，保障 C 端时间流转闭环）。
- **高级配置折叠收起**：
  - `<el-collapse>` 折叠面板默认收起，内置最智能默认值：试卷标题（默认 AI 命名）、试卷分类（默认第一项）、难度（默认中等）、限时（默认与窗口对齐）、及格线（默认60%）。
- **生成机理定义（大模型全量真生成）**：
  - 明确为**大模型实时全量原创生成**（拒绝题库翻炒）。试卷保存时，这批高质量题目同步沉淀入库共享题海。
- **生成后所见即所得审阅视图**：
  - 生成完毕后原地展开审阅卡片，复用上述“选项与正确答案绿色高亮”排版，老师肉眼核对无误后，手动点击 **[确认保存试卷]** 正式落库。

---

## 5. B 端 AI 助管 (Copilot) 工业级交互规范

### 5.1 视觉风格与厂商脱敏
- **模型厂商全站脱敏**：界面统一展示为 **「AI 智能助管」** 或 **「AI 智算引擎」**，严禁露出底层任何具体供应商商业名称（如 SenseNova、Agnes、DeepSeek 等）。
- **现代消息气泡质感**：
  - AI 回复气泡采用柔和浅灰底（`#f8fafc`）配 1px 浅灰细边框（`#e2e8f0`），顶部带引用条：`| 回复 全Ai系统: [用户提问摘要]`。

### 5.2 人机协同确认卡片 (Action Card)
- 当用户提问涉及管理写操作（如管理员向出题人划拨额度、批量把生成的题目存入题库）时，AI 输出结构化红调卡片：
  - 标题：`确认: xxx 业务操作 [待确认]`
  - 元数据：风险等级 (`riskLevel: medium/high`)、操作明细、有效时间。
  - 按钮组：红色主按钮 `[确认执行]` 与次按钮 `[取消]`。
  - 点击确认后调后端真实接口，状态流转为绿色 `已执行`，按钮消失防重放。

### 5.3 交互式操作路由卡片 (Interactive Action Cards) 与严格权限边界
- **业务场景 (以待办查询为例)**：
  - 出题人在 AI 对话框提问：“我有几套题目没有批改？”
  - AI 回复时附带一张**结构化任务卡片**（列出待批阅试卷清单、待批数量），右侧配有操作条按钮：`[去批改]`。
  - 用户点击 `[去批改]`，**直接在系统内原地唤出该试卷的主观题批阅抽屉**（无需跳出聊天窗，零中断批阅）。
- **三类通用交互协议**：
  1. `in_app`：站内组件/抽屉原地唤起或路由下钻（如 `open_grading_drawer`）。
  2. `modal`：弹窗二次确认或纯文案说明展示。
  3. `external`：安全新标签页打开外部文档链接。
- **🚫 严格权限边界与安全白名单 (Permission Boundaries)**：
  - **私有数据隔离**：出题人向 AI 询问待办时，前端注入的快照严格仅包含其自身创建的试卷（`creator_id == current_user.id`），AI 输出也仅限其名下试卷。
  - **底层安全拦截**：即使出题人尝试唤起非本人创建的试卷，批阅抽屉调后端接口时也会因归属不符返回 `403 Forbidden`。
  - **前端动作白名单**：前端预设静态动作映射字典，出题人角色严禁通过 AI 卡片触发人员管理、配额划拨或全局看板动作，越权指令直接拒绝渲染按钮。

### 5.4 知识盲区与超纲优雅降级
- 遇到未知私有数据或外部超纲提问，AI 严格遵循 System Prompt 事实约束，诚实告知“超出当前题库知识库范畴”，杜绝幻觉编造。
- 气泡底部附带 3 个动态推荐提问胶囊（Pill Tags），引导回到有效业务问答。

### 5.5 算力资产化与流式传输
- **内外隔离**：
  - 主动交互（AI 助管聊天、AI 一键组卷）严格扣除登录者个人的 `daily_ai_quota`。
  - 被动业务（学生交卷触发的后台批阅）消耗系统公用大盘，不扣个人额度。
- **流式传输**：基于 Server-Sent Events (SSE) 协议推流，打字机实时呈现。

### 5.6 企业级会话持久化、跨端漫游与微信式向上游标分页
- **服务端持久化（告别脆弱的 localStorage）**：
  - 会话与对话流水全量入库（`ai_chat_sessions` 与 `ai_chat_messages`），实现真正的多端数据漫游（换浏览器、换电脑登录，历史会话无缝同步）。
  - 支持会话重命名、会话删除、新建会话。
- **仿微信向上滑动游标分页 (Cursor-based Pagination)**：
  - 首次进入会话：仅加载最新 20 条消息，自动平滑滚至最底部（展示最新问答）。
  - 向上滑动画卷：当滚动条距离顶部不足 60px 时，按游标（`before_id`）按需向前拉取更早的 20 条历史记录。
  - **视口无感锚定算法 (Scroll Anchoring)**：加载旧记录后，通过滚动高度差值补偿（`scrollHeight - oldScrollHeight`），视口保持原地不动，绝对杜绝向上翻页时的画面跳动与卡顿。

---

## 6. v1.3 个性化智能体与模型枢纽架构 (v1.3 新增)

### 6.1 Mem0 个性化记忆引擎 (Personalized Agent)
- **底层架构**：放弃基于 Session 的短期记忆，引入 `Mem0` 引擎进行基于 `User_ID` 的长期记忆持久化。
- **业务场景**：管理员/出题人在聊天窗口随口表达的教学偏好（如“我教高三数学”、“我倾向出极难的题”），后台静默提取为向量记忆。
- **无缝注入**：下次新建对话或生成试卷时，大模型基于该记忆自动拼装专属 Prompt，实现“越用越懂你”的专属 AI 体验。

### 6.2 动态模型中心 (Model Center)
- **卡片化呈现**：摒弃硬编码 API，前台新增【模型中心】模块，使用高定“渐变色卡片”网格展示。
- **动态拉取机制**：支持输入自定义大模型通道 BaseURL 和 API Key，一键拉取通道下的全部模型并填充至下拉框。
- **全局状态共享**：前端利用 Pinia 存储用户当前激活的主力模型（允许随时热切换云端模型与本地 3070 模型），为系统提供高度灵活性。

### 6.3 专属私有文档库与 RAG 溯源侧边栏
- **前端交互**：上传 PDF/Word 等私有资料库时，提供流式切片进度 UI 渲染。
- **原生向量存储**：放弃冗重的第三方向量库，利用 TiDB Cloud Serverless 内置的 Vector 数据类型实现极简文本/向量同表绑定。
- **高阶溯源呈现**：基于资料生成的题目会自带 `[引用溯源]` 标签，点击在页面右侧唤起抽屉（Drawer），并**精准高亮显示原文档的相关出处**，提供满分级所见即所得体验。

### 6.4 Dashboard 看板真实数据流闭环
- **API 聚合展示**：首页图表（KPI、试卷折线趋势、排行等）从 Mock 升级为后端聚合 API，打通真实业务流水线。

---

## 7. C 端考生全状态流转架构 (闭环)

### 7.1 “我的测试”三态流转
底部 TabBar 增加【我的测试】，顶部提供状态 Tabs：
1. **进行中**：当前时间在 `start_time` ~ `end_time` 内。
   - **时间硬倒挂压缩**：距 `end_time` 仅剩 10 分钟时，倒计时强制压缩为 10 分钟。
   - **逃逸异常懒计算 (Lazy Evaluation)**：学生意外关停卡在 `in_progress` 时，下次拉取列表顺手结算为 `已交卷`，0 运维成本。
2. **未开始**：当前时间未到 `start_time`，卡片按钮置灰禁用，提示“考试尚未开始”。
3. **已考试**：
   - **最新记录聚合**：卡片永远展示考生最新一次作答的状态与得分。
   - **防泄题锁死**：只要当前时间未超过绝对 `end_time`，**`查看解析` 按钮强制置灰禁用**，杜绝考场泄题。
