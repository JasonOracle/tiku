# 智题库 (TiKu) 软件需求规格说明书 (PRD)

---

## 1. 执行摘要与项目概述

### 1.1 项目背景
智题库（TiKu）是一款面向大众年轻用户的轻量级在线测评与考试平台。项目采用 C 端移动端 (Vue3 + Vite + SPA) + B 端 SaaS Web 管理后台 + Python (FastAPI) 后端架构。
项目旨在为用户提供极佳的刷题与知识测验体验，同时作为开发者个人全栈与 AI 编程落地的示范项目。

### 1.2 目标与成功指标 (KPIs)
- **交付目标**：MVP 阶段实现完整的用户答题闭环、错题分析报告、题目收藏，以及 B 端题库管理与 Excel 导入。
- **性能指标**：支持百人规模并发作答，C 端页面首屏加载时间 < 1.5 秒，API 响应时间 < 200ms。
- **验证标准**：100% 通过自动化功能测试与接口集成验证。

### 1.3 迭代规划 (V2 Roadmap)
> **🚧 MVP 说明**：当前版本为快速验证核心逻辑的 MVP 产品，仅支持客观题型（单选、多选、判断）和自动阅卷。
> 
> 下个主要版本 (V2) 将计划引入以下高级功能：
> 1. **主观题型支持**：新增简答题、填空题。
> 2. **人工阅卷**：B 端后台引入考卷人工批改工作流。
> 3. **多账号权限管理 (RBAC)**：支持不同角色（超级管理员、出题老师、阅卷老师）的细粒度权限控制。

---

## 2. 视觉设计系统规范 (Design System)

根据用户选定的设计语言与 `direction-approved.md` 规范：
- **设计风格**：现代浅蓝 & 柔和渐变毛玻璃 (Light Aero Glass Design)
- **色彩规范**：
  - 主品牌色：天空湛蓝 `#0284C7` (oklch 优化)
  - 辅助渐变：`linear-gradient(135deg, #38BDF8 0%, #818CF8 50%, #C084FC 100%)`
  - 毛玻璃卡片：`rgba(255, 255, 255, 0.65)` + `backdrop-filter: blur(16px)` + 白透发光边框
  - 全局背景：天蓝渐变 `#E0F2FE` ➔ `#F8FAFC`
- **排印系统**：`Plus Jakarta Sans` / `Outfit` / `Inter`

---

## 3. C 端移动端功能规格

### 3.1 首页 (Home)
- **Banner 与推荐轮播**：支持 Banner 动态数组（图片 URL、跳转类型、跳转路径），0 张时降级显示 Hero 蓝紫渐变推荐卷卡片。
- **分类导航**：支持横向毛玻璃 Pill 分类滚动单选（如：热门推荐、消防安全等）。
- **试卷列表**：展示试卷标题、三栏统计指标（题目数量、满分、及格分），并展示 SVG 矢量插画封面。
- **导航 (TabBar)**：底部 TabBar 仅保留“首页”与“个人中心”。

### 3.2 答题流程 (Quiz Engine)
- **顶栏状态**：全局 NavBar 提供返回箭头、试卷标题（超长截断）、`1/4` 进度。
- **试卷配置支持**：
  - 支持后台配置是否乱序（`is_random`），开启时 C 端进入答题将随机打乱题目排列。
- **模式支持**：
  - 限时考试（显示顶部动态倒计时，<3分钟变红，超时自动强制交卷）
  - 不限时练习（展示“练习模式”标签）
- **交互控制**：
  - 题目按顺序呈现，单选/多选/判断交互采用大圆角卡片。
  - 底部操作区支持“上一题”返回修改与“下一题/交卷”。
  - 右上角支持一键收藏/取消收藏当前题目（若当前题目已被收藏，则彻底隐藏收藏图标）。
  - **全局弹窗**：系统提示、离开确认、交卷确认等统一使用毛玻璃风格 `AppModal.vue` 弹窗，替代原生 `confirm`/`alert`。
  - **防丢卷与空卷**：做题中离开提示并自动提交已答卷；一题未答时离开/超时均不提交（不产生 0 分记录）。

### 3.3 考试分析报告 (Analysis Report)
- **成绩统计**：
  - SVG 绿色渐变环形仪表盘（展示得分），辅以总分与合格线。
  - 三列统计卡片：答对题数、答错题数、作答用时。
- **错题解析**：展示所有题目明细（答对标记，答错标红），清晰对比用户答案与正确答案，并附带解析说明。

### 3.4 个人中心与收藏记录 (Profile & History)
- **个人中心 (ProfileView)**：
  - 顶部展示大渐变 SVG 头像与用户名。
  - 仪表盘展示 2 个核心统计卡片：`累计作答场次`、`综合通过率`。
  - 提供导航 Cell 列表：
    - **我的题目收藏夹**：跳转至独立收藏夹列表 `/favorite`。
    - **历史答题记录**：跳转至历史答卷列表 `/history`；点击记录卡片跳转报告 `/report/:record_id`。
    - **退出登录**：置于 Cell 列表底部。

---

## 4. B 端 SaaS 管理后台功能规格

### 4.1 首页 Banner 管理（新增）
- **Banner 配置**：最多 3 张启用，超限时拦截；支持配置图片 URL（复用上传功能，建议 16:9）、链接跳转三态（无跳转 / 外部 URL / 内部站内路径，如 `/quiz?exam_id=1`）。
- **轮播控制**：提供全局轮播秒数调节（2–10s，默认 4s）。

### 4.2 题海管理 (Question Pool)
- **题目分类**：左侧菜单层级必须将“分类配置”放置在“题海管理”上方。绑定题目专用分类（`target_type='question'`），支持按分类、题型、中文难度筛选；新建或导入题目必须强制校验是否存在分类，若无分类则弹窗阻断并引导至分类管理页；题目弹窗的所属分类为必填项。
- **题目 CRUD**：配置题干、选项、标准答案、解析、难度及默认分值（默认10分）；被已上架/归档试卷引用的题目不可删除（仅草稿引用时联动移除）；题库列表支持 `checkbox-group` 多选批量删除功能并前置确认。
- **批量导入**：支持 Excel 按“分类”列逐题归入；支持题干、选项、答案、解析、难度、分数；跳过预留的简答/填空题型。

### 4.3 试卷管理 (Exam Management)
- **封面预设**：内置 5 套矢量 SVG 封面（盾牌/灯笼/书卷/奖杯/星空），建卷单选（默认 preset:1），存为 `preset:N`。
- **试卷分类**：绑定试卷专用分类（`target_type='exam'`）。草稿分类悬空时强制重选。
- **状态流转（三态）**：
  - `draft` 待上架 / `published` 已上架 / `archived` 已归档冻结。
- **试卷分类**：必选试卷专用分类（`target_type='exam'`）；上架时写入分类名称快照，分类改名/删除后已上架/归档卷照常显示原文；草稿分类悬空时置空并强制重选，无可选分类不许上架。
- **上下架状态控制（三态，归档为彻底终态）**：
  - `draft` 待上架 / `published` 已上架 / `archived` 已归档冻结（不可再编辑/删除/上架）。
  - 快捷上架/下架切换，下架即归档，下架时弹出 Confirm 确认弹窗（明确告知冻结后果）。
- **组卷管理与批量操作**：
  - **备选题库区**：升级为独立弹窗形态，支持分页、搜索、题型过滤及 `checkbox-group` 多选一键加入。
  - **已加入题目区**：支持 `checkbox-group` 复选批量移除，支持拖动题目或点击按钮调整作答顺序；可开启“随机打乱题目顺序”（`is_random`）功能。
- **编辑锁定与考情看板**：
  - **已上架试卷**：硬性锁定组卷配置（保护历史成绩数据一致性）。
  - **试卷详情弹窗**：支持通过展开行（`expand`）完整展示单题的选项、标准答案与文字解析。
  - **考情数据看板**：上架/归档卷均可查看：参与作答人数、平均得分、通过率，以及全量用户作答明细表格。0 作答的误入不产生记录（空卷不交卷）。
- **删除规则**：仅草稿零作答可删；有作答、上架中、已归档一律不可删（有作答者指引下架归档）。

### 4.4 分类管理 (Category Management)
- **双分类隔离**：分类配置页面提供 Tabs 切换：
  - **题目分类**：用于题海题目归类（如：金融类、消防安全、编程基础）。
  - **试卷分类**：用于试卷归类，用于 C 端横向筛选导航。
- **关联保护**：存在正被使用（关联题目/试卷）的分类不可删除；如果尝试删除，需提示用户“该分类下仍有关联数据，无法删除”。
- **用户与记录管理**：查看 C 端注册用户列表及全站答题记录明细。

---

## 5. 技术架构与数据库设计

### 5.1 技术栈
- **C 端**：UniApp (Vue3) / H5
- **B 端**：Vue3 + Pinia + Element Plus
- **后端**：Python 3.12 + FastAPI + SQLAlchemy ORM
- **数据库**：MySQL 8.0
- **部署**：Docker + docker-compose (Debian NAS)

### 5.2 核心数据表结构
1. `users`：C端用户表（id, username, password_hash, status...）
2. `admins`：B端管理员表（id, username, password_hash, role...）
3. `exam_categories`：试卷分类表（id, name, icon, sort_order...）
4. `questions`：题目表（id, type, title, options[JSON], answer[JSON], explanation...）
5. `exams`：试卷表（id, title, category_id, is_timed, time_limit, total_score...）
6. `exam_questions`：试卷-题目关联表（id, exam_id, question_id, score, sort_order）
7. `exam_records`：答题记录表（id, user_id, exam_id, status, time_spent, answers[JSON]...）
8. `user_favorites`：用户收藏表（id, user_id, question_id）

---

## 6. 开发与测试硬性规定

- **工程交付红线**：每次改动后必须编写/更新测试，交付前确保所有单元测试与接口集成验证通过。
- **安全与认证**：C端与B端均基于 JWT Bearer Token 进行身份鉴权；密码采用 bcrypt 强哈希存储。
