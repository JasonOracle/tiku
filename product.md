# 智题库 (TiKu) v1.2 产品需求文档 (PRD)

## 1. 目标与背景
基于 v1.0 MVP，v1.2 将全面贯彻**“AI 平权”与“五可架构”**理念，引入权限管理、主观题型，并将 AI 作为真实的“数字员工”融入业务流（阅卷与出题），打造真正的 AI-Native 测评系统。

## 2. 核心架构决议 (已对齐)

### 2.1 权限管理 (RBAC) 
- **角色架构**：超级管理员 (Super Admin)、普通管理员 (Normal Admin, 老师)、AI员工 (AI Employee)。
- **数据可见性**：
  - **题海池共享**：所有老师共享题库资源。
  - **试卷隔离**：老师只能管理和批改自己创建的试卷。
- **题目锁定防篡改**：一旦题目被处于“已上架”或“已归档”的试卷引用，该题目将进入“全局只读”状态，不可修改（若需修改只能复制产生新题），杜绝历史数据和正在进行的考试发生崩塌。
- **公共题库的“防牵连”软删除 (Soft Delete)**：在“共享题库”模式下，题库中的题目绝对不允许物理删除，只能打上 `is_deleted = 1` 的软删除标记，前端题库不再展示，但已引用的试卷依然能正常拉取到原题。

### 2.2 题型扩展与判定规则
- **单选/判断题 (客观判卷)**：100% 强匹配判卷。
- **多选题半对机制**：开启多选题“漏选得一半分，错选不得分”的底层算分逻辑。
- **填空题 (客观判卷)**：
  - 支持“一空多答”（如配置 ["北京", "北京市"] 均算对）。
  - **题干与答案结构**：题干一律使用三个下划线 `___` 作为前端渲染占位符。答案字段为**二维数组**。
  - **防崩溃强制校验**：在出题保存时，后端必须强制校验 `题干中 ___ 的数量 == answer 二维数组的长度`。
  - **无需 AI 阅卷**：系统底层采用传统代码执行匹配，对比前切除首尾空格并统一大写。
- **简答题 (主观判卷)**：
  - 教师端录入题目、标准答案和“踩分点”。
  - 学生交卷后进入“待批改”状态。

### 2.3 AI 阅卷 (人机协同大厅与全托管模式)
- **试卷级全托管配置 (Auto-Approve)**：在 B 端**创建/编辑试卷的弹窗**中，新增开关 `[是否开启 AI 全权阅卷]`（对应字段 `is_ai_auto_grade`）。
  - **动态显示逻辑**：该开关仅在试卷中**包含简答题时才显示**。
  - **全权托管 (开启)**：交卷后，AI 自动批阅简答题，完成后直接发布总成绩，无需老师复核。
  - **预批改模式 (关闭)**：AI 仅生成建议分数和评语，试卷进入“待复核”状态，需由老师在阅卷大厅逐一确认。
- **原子化 Prompt 与动态满分 (底层机制)**：
  - AI 阅卷时，传入当前试卷中的实际分值 (`exam_questions.score`)，按动态满分算分。
  - 单份试卷的所有主观题必须合并为一个大 JSON Prompt 发起**单次大模型请求**，保证阅卷数据的原子性（同成功同失败）。
- **阅卷大厅 UI**：展示题目、踩分点、学生原文、AI 判卷结果卡片。支持一键“采信全部 AI 评分”。

## 3. AI 自动出题与组卷 (AI Generator)

### 3.1 AI 生成题目 (Question Generation)
- **交互方式**：在 B端“题海管理”页面点击【✨ AI 出题】。提供材料文本或自然语言描述。
- **底层机制**：AI 生成结构化 JSON 后调用“新增题目 API”。
- **风险控制**：生成的题目带有“AI生成”专属标签，强制要求老师在前端预览弹窗进行“入库前二次确认”。

### 3.2 AI 智能组卷 (Exam Generation)
- **交互方式**：在“试卷管理”页面点击【✨ AI 智能一键组卷】。输入试卷整体参数和题型要求。
- **底层机制**：AI 优先从当前共享题库检索符合条件的题目；若数量不足，自动生成新题补齐；随后生成试卷记录。
- **风险控制**：试卷生成后，系统强制将其置为 **Draft (草稿)** 状态，必须由人类老师检查并手动上架。

## 4. 消息中心与 AI Copilot

### 4.1 系统消息与通知流
- **站内信机制**：B 端增加“消息中心”模块。联动业务场景，如 AI 批量预批改完成通知、智能组卷草稿生成通知等。

### 4.2 B 端 AI 助手 (Copilot / AI 工作台)
- **前端状态快照注入 (Frontend State Preamble)**：
  - 侧边栏助手不进行复杂的后端数据库直连查询。
  - 提问时，前端动态将当前 Vuex/Pinia 中的状态（当前老师身份、仪表盘数字、当前停留页面）拼接为隐藏的 Markdown 文本塞入 Prompt，使 AI 拥有轻量级的极低成本“上帝视角”。
- **权限安全**：AI 助手能力受限于注入的上下文，绝不允许越权查阅系统敏感信息。

### 4.3 双域留痕与高危阻断 (Audit)
- **双日志全量留痕**：任何发生写操作（增删改）的接口调用（无论人类还是AI），必须完整记录在 `audit_logs` 表中：`时间、操作人ID、动作类型、操作前快照、操作后数据`。

## 5. MVP 商业化调度考量

### 5.1 轻量异步与降级兜底
- **异步批阅**：使用 FastAPI 原生的 `BackgroundTasks` 执行 AI 阅卷，避免引入 Celery/Redis 等重型基建。
- **全托管宕机兜底**：若大模型 API 宕机导致 AI 全权阅卷失败，系统自动将失败试卷退回 B 端列表，亮起 `[异常:N份待批阅]` 红点，允许人工接管或重新触发 AI。

### 5.2 AI 算力资产化隔离
- **个人额度管控**：超管为每位老师分配 `daily_ai_quota`（每日主动生成额度）。老师主动发起的“AI 出题”、“AI 组卷”、“AI 聊天”严格扣减该额度，余额不足则拦截。
- **系统业务兜底**：学生交卷触发的“被动 AI 阅卷”属于系统核心业务职责，消耗统一记录在系统账单中，**不扣减老师个人额度**。
- **极简 AI 配置**：MVP 阶段后台无需复杂的模型切换网关，直接在后端 `.env` 中写死主力大模型的 API Key。

## 6. C / B 端体验联调与状态流转

### 6.1 C 端“我的测试”流转架构
底部的 TabBar 增加【我的测试】，通过顶部 Tabs 过滤试卷：
1. **进行中**：
   - **进入条件**：当前时间在试卷 `start_time` 和 `end_time` 之间。
   - **时间硬边界 (Hard Boundary)**：若距 `end_time` 仅剩 10 分钟，哪怕试卷限时 60 分钟，倒计时强制压缩为 10 分钟。到达 `end_time` 系统立即强制收卷。
   - **逃逸异常懒计算**：若学生中途断网关停，卷子卡在 `in_progress`。此时无需后端定时跑批，采用“懒惰求值”：当学生下次打开列表或老师打开阅卷大厅拉取数据时，如果发现该卷子时间已超时，系统顺手将其强制扭转为 `已交卷`。
2. **未开始**：当前时间未到 `start_time`，卡片按钮置灰禁用，提示“未开始”。
3. **已考试**：
   - **多记录聚合**：允许次刷题。列表按试卷卡片聚合展示，卡片状态（批阅中/已通过/未通过）永远只反映**最新一次作答**的成绩。
   - **防泄题锁死**：只要当前时间未超过试卷的绝对 `end_time`，即使提前交卷出分，**`查看解析` 按钮也强制置灰禁用**。

### 6.2 B 端试卷管理视图对齐
1. **组卷时间锁强制校验**：
   - 必填 `start_time` 和 `end_time`。
   - 严格校验：`考试时长 (time_limit) ≤ (end_time - start_time)`，违背则前端报错拦截。
2. **试卷列表的双维状态展示**：
   - 主状态：草稿 (Draft)、已上架 (Published)、已归档 (Archived)。
   - 子状态（仅上架后）：动态 Badge 提示 `[未开始]、[进行中]、[已结束]`。
3. **待办红点机制**：试卷列表中，若存在待阅卷的考卷，对应行高亮 `[N份待批阅]` 按钮，一键跳转过滤好的阅卷大厅。
