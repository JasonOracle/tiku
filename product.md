# 智题库 (TiKu) v1.4+ 产品需求文档 (PRD)

> **版本定位**：v1.4+ 是系统的重大成熟里程碑。本项目已全面演进为**支持多租户组织物理隔离的企业级智能考核、测评与题库 SaaS 平台**。平台兼顾严谨的考核测评场景与灵活的组织协同，全面贯通**试卷资产全周期、AI 全景感知与只读聚合工具链、Mem0 本地长期记忆、以及多租户公私知识库**。

---

## 1. 核心业务名词与定位规范 (Terminology Standards)

经过产品多轮实战迭代，界面与业务语言终结了早期过度泛化（“任务/条目/核验”）带来的交互晦涩，**全面确立为直观、精准的业务概念**：

| 平台模块 / 概念 | 标准业务名词 | 英文标识 | 职责定义 |
| :--- | :--- | :--- | :--- |
| **试卷管理** | 试卷 / 考试测评 | `tasks / exams` | 考核评测载体，支持草稿、已发布（已上架）、已归档流转，支持限时与防作弊 |
| **题目管理** | 题目 / 题库资源 | `resources / questions` | 试题资产库，支持单选/多选/判断/简答，支持知识库切片溯源强绑定 |
| **阅卷管理** | 阅卷 / 测评批改 | `verification / grading` | 客观题自动批改，主观题支持 AI 预阅卷 + 管理员终审采信 |
| **AI 知识库** | AI 知识库 / 文库 | `kb (knowledge base)` | 企业私有知识切片库，支持公私域隔离、多格式提取与独立无污染召回调试 |
| **企业管理** | 企业管理 / 租户管理 | `super / tenants` | 超级管理员专用上帝视图，管理全平台企业租户的创建与状态监控 |
| **成员管理** | 组织成员 / 成员档案 | `members` | 企业内部用户管理，支持静默建号与全量画像（性别/年龄/职业/简介） |
| **C端考生端** | 测评中心 / 我的考试 | `member / toc` | 组织内普通成员作答中心，展示当前企业上架试卷并提供计时答题 |

---

## 2. 身份与多租户隔离体系 (Multi-Tenant RBAC)

### 2.1 极简入驻与做题闭环流转
彻底杜绝无组织挂靠的“悬空账号”：
1. **统一添加**：企业管理员在 B 端「成员管理」中输入成员手机号、姓名及职业画像，点击添加。
2. **静默录入**：若该手机号未注册，系统自动静默创建账号并设置默认密码（`123456`），同时将该成员安全挂载到当前企业的 `tenant_id` 下，角色分配为 `member`（普通成员）。
3. **C 端答题闭环**：
   - 成员使用手机号与密码登录 C 端（`toc` 测评前台）。
   - C 端自动加载当前企业中所有**已上架（`published`）**的试卷。
   - 成员点击作答，系统在服务端锁定开考时间戳（防前端篡改作弊），提交后数据自动推入 B 端「阅卷管理」池。

### 2.2 租户角色与上帝视图
- **超级管理员 (Super Admin)**：
  - 拥有全平台企业管理权限。默认无租户归属，需选择具体企业（如“星雅教育”或“皓石集团”）进入**视察模式**。前端请求头强制挂载 `X-Tenant-ID` 穿透访问，严禁在后端业务逻辑中开设特权后门。
- **企业所有者 / 管理员 (Owner / Admin)**：
  - 拥有所属企业的数据全权管理能力：试卷编排、题目入库、阅卷裁决、团队画像维护、公共知识库维护以及企业 AI 助手协同。
- **企业成员 (Member)**：
  - 参与试卷作答、查看成绩与解析、维护个人私有知识库。

---

## 3. AI 助理生态：双类工具链与企业全景感知

AI 助理不再是单一的聊天窗口，而是深度嵌入业务底座的**企业智能助管**：

### 3.1 工具体系分类设计
AI 助手挂载的工具严格遵循“读写分离与安全交互”原则，分为两类：

1. **只读聚合工具 / 感知器 (Read-only Aggregation Tools) — 静默执行，自动结构化回传**：
   - **`_get_tenant_organization_snapshot`（企业团队人员花名册感知）**：
     - 自动聚合当前企业在职人员名单、角色（所有者/管理员/教师/成员）、联系方式以及全量画像（职业、性别、年龄、个人简介）。
   - **`_get_tenant_tasks_snapshot`（企业试卷资产全景感知）**：
     - 自动聚合企业试卷总数、各老师/出题人的出卷统计数量、以及最近出卷的详细清单（包含出卷人、题目数、阅卷模式、创建时间与状态）。
   - **核心特性**：由执行器在推理初始化时**自动静默完成结构化聚合**，作为权威事实注入上下文，彻底消除模型在回答“我们有几个人”、“xx老师近期出了什么卷子”时的幻觉，无需用户点击确认。
2. **写操作工具 (Write / Action Tools) — 交互卡片，用户终审确认**：
   - `create_exam_draft`（智能组卷草稿生成）
   - `create_question_draft`（单题 / 批量出题草稿）
   - `delete_exam`（试卷安全删除）
   - `delete_question`（题目安全归档）
   - **核心特性**：模型输出工具参数后，前端渲染 Ant Design X 风格预览卡片，用户可视化核对参数后点击“立即执行”，调用后端正式落库，杜绝不可逆误操作。

### 3.2 Mem0 本地长期记忆体系 (Zero-Cloud Long-Term Memory)
- **零外部网络依赖**：AI 对话深度集成纯本地持久化的 Mem0 长期记忆网络，自动在后台异步提取并沉淀用户的对话习惯与出题偏好。
- **多租户隔离沉淀**：记忆键强绑定 `tenant:{tenant_id}:user:{user_id}`，保证企业与企业、成员与成员之间的偏好记忆互不干扰。
- **生命周期级联清理**：当成员从企业注销或物理删除时，系统自动触发 Mem0 向量底座的对应记忆清除。

---

## 4. AI 知识库 (KB)：明亮科技风重构与功能收敛

AI 知识库经历了从旧版实验性 RAG 到现代化工业级面板的彻底重构：

### 4.1 页面合并与轻量交互
- **指标与文件合一**：在知识库顶部直接外显 **4 块动态度量卡片**（文档总量、向量切片总数、就绪状态、存储空间渐变容量条），淘汰了独立的统计二级页面。
- **公私域快捷切换**：顶层提供「公共知识库」与「私有知识库」的即时 Switch，一键切换操作作用域（`scope: 'public' | 'private'`）。

### 4.2 功能删除与解耦：独立的抽屉式 RAG 验证面板
- **彻底删除旧版聊天混入**：废除了旧架构中“知识库检索测试混入 Copilot 会话历史”的做法，彻底杜绝测试召回污染日常对话列表。
- **右侧抽屉式调试器**：点击“测试召回”，滑出专属调试抽屉，调用专属独立接口 `/api/v1/admin/kb/query` 进行检索打分与切片命中展示，**不落会话数据库**，即测即看。
- **多格式切片支持**：原生支持 DOCX, PDF, XLSX, TXT 等文件的纯文本提取与切片分块，切片列表直观外显解析进度与彩色格式徽章。

---

## 5. 基准运行租户标准

系统出厂内置并清洗固化了两个标准基准租户，供功能验证与验收：
1. **星雅教育**（Tenant ID: 1）
   - 场景：典型培训与考核机构。包含完整教师团队、管理人员与多套已上架教学评测试卷。
2. **皓石集团**（Tenant ID: 2）
   - 场景：企业内部员工合规与技能考核。包含企业员工名册与岗位培训考卷。
