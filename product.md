# 智题库 (TiKu) v1.4 产品需求文档 (PRD)

> **版本定位**：v1.4 是本项目的重大分水岭。我们正式剥离“教育/考试”专有属性，彻底转型为 **多租户隔离的通用型 B2B 企业管理 SaaS 平台**（类钉钉/飞书模式）。所有 UI 展示、交互链路和业务逻辑，必须完全通用化。

---

## 1. 核心业务名词重塑 (Terminology Restructuring)

为了实现通用化，系统内所有文案、提示词及底层接口变量名，强制执行以下映射：
- ❌ `教师/出题人 (Teacher/Creator)` ➔ ✅ **`管理员 (Admin)`**
- ❌ `学生/考生 (Student)` ➔ ✅ **`成员 (Member)`**
- ❌ `试卷/考场 (Exam)` ➔ ✅ **`任务 / 测评 (Task / Assessment)`**
- ❌ `题目 (Question)` ➔ ✅ **`条目 / 资源 (Item / Resource)`**
- ❌ `题库 (Question Bank)` ➔ ✅ **`资源库 (Resource Library)`**
- ❌ `阅卷 (Grading)` ➔ ✅ **`数据审批 / 核验 (Verification)`**

---

## 2. 身份与多租户隔离架构 (Multi-Tenant RBAC)

### 2.1 极简入驻流 (B端建号 -> 自动绑组织)
彻底废除传统的 C 端开放注册（`/register`），杜绝“无组织悬空账号”。
1. **统一入口**：B端企业管理员在后台“成员管理”模块，通过输入手机号添加成员。
2. **静默录入与自动绑定**：系统检测该手机号若未注册，则自动静默创建账号，并初始化默认密码（如 `123456`）。无论账号是新建还是已有，均强制挂载到当前操作者所属的 `tenant_id`。
3. **闭环登录**：成员（C端用户）收到通知后，使用手机号+默认密码登录系统，由于底层数据已强绑定，登录后直接进入该企业的专属空间。

### 2.2 上帝视角与企业级权限
- **超级管理员 (Super Admin)**：
  - 不归属任何企业（`tenant_id` 概念为空）。
  - 拥有专用的上帝路由 `/super-admin/tenants`，可管理全平台租户（创建企业、封禁企业）。
  - **视察模式**：点击“进入企业”后，前端需主动拦截请求，在 API Header 挂载该企业的 `X-Tenant-ID`，以此复用标准的业务接口，**严禁在后端业务接口写特权放行代码**。
- **租户管理员 (Tenant Admin/Owner)**：
  - 只能在自己的 `tenant_id` 数据沙箱内操作。
  - 统管当前组织的资源库、任务分发、成员名册及 AI 知识库。
- **租户成员 (Tenant Member)**：
  - 只能接受任务、提交结果，并管理个人的私有 AI 知识库。

---

## 3. AI 知识库分权与溯源 (Public vs Private RAG)

### 3.1 知识库双轨制
知识库不再是单一的“上传文件就完事”，而是强关联业务权限：
1. **公共知识库 (Public KB)**：
   - 仅限 **租户管理员** 维护（如上传企业规章制度、产品白皮书）。
   - 对当前租户下所有成员**只读共享**。
   - 组织级 AI 助手的基准事实来源。
2. **私有知识库 (Private KB)**：
   - 所有成员皆可维护个人私有区（如个人工作笔记、学习心得）。
   - 数据**绝对物理/逻辑隔离**，甚至连租户管理员也无法读取成员的私有知识库。
   - 个人 AI 助手的补充知识源。

### 3.2 强信任机制：切片级溯源 (Traceability)
为了打破大模型“胡编乱造”的黑盒：
- 当 AI 基于知识库（RAG）自动生成任务（出题）或回答提问时，系统后端必须将命中检索的**文档原文切片 (Chunk)**、**来源文件名** 及 **匹配度分值** 以 JSON 格式一并落库。
- **前端展现**：无论是在任务执行页面还是 AI 聊天面板，AI 生成的内容旁必须高亮显示 `[📚 参考溯源]` 标签，用户点击可直接滑出原文对比，建立企业级使用信任。

---

## 4. 核心业务流程：任务的创建与分发 (Task Lifecycle)

原先的“组卷与考试”流程，现抽象为通用的**“资源整合与任务下发”**：

1. **资源录入 (Items Creation)**：
   - 管理员手动或通过 AI，将具体的事务、考核点录入“公共资源库”。
2. **任务编排 (Task Assembly)**：
   - 管理员从资源库中挑选条目，组合成一个完整的任务包 (Task)。
   - 配置任务属性：开始/结束时间、是否要求必须验证（原自动批阅/人工批阅）。
3. **下发与执行**：
   - 成员在 C 端（或前台页面）看到属于自己的待办任务。
   - 在规定时间窗内执行任务并提交。
4. **数据核验 (Verification)**：
   - 提交后的任务进入“核验大厅”。
   - 引入 AI 异步核验（原 AI 批改），生成结构化建议，管理员一键采信或驳回，完成业务闭环。
