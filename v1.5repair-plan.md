# TiKu v1.5 架构缺陷最终修复计划 (v1.5repair-plan.md)

> **前言**：本方案基于 WorkBuddy 自动化核心场景测试报告 (`test-report.md`) 暴露出的 5 个底层漏洞，并结合了极其严苛的源码级审查后定稿（v1.5.1 精准矫正版）。我们彻底抛弃了脱离源码臆测表结构的错误思路，将修复策略精准定位到了具体的方法签名与网关配置上，确保 100% 具备工程落地可行性。

---

## 🛠️ 四大重构策略终极矫正

### 1. Nginx 限流防刷：多级映射兜底 (LLM-SYS-01, P0)
- **过往误区**：错用租户级 Key 导致同校连坐封号，放过 Login 接口（由于无 Token 不限流），且漏写 HTTP 429 状态码。
- **精准矫正执行**：
  1. **多级映射 (Map Fallback)**：在 `nginx.conf` 的 `http` 块中使用 `map` 指令。如果有 `$http_authorization` (JWT)，则将其提取为限流 Key；如果没有（如针对 `/api/v1/auth/login` 爆破），则自动降级为 `$binary_remote_addr` (真实 IP)。
  2. **状态码覆写**：必须显式配置 `limit_req_status 429;`，确保前端捕获标准防刷码。
  3. **精准圈定**：绝不搞全局限流误伤 C 端答题。仅将 `limit_req` 挂载到 `/api/v1/saas/ai/`、`/api/v1/admin/ai/` 和 `/api/v1/auth/` 这三个高危高消耗的特定 `location` 块中。

### 2. C 端生命周期与缺考：补行与“三重门”防线 (APP-02 & APP-05, P1)
- **过往误区**：未意识到未开考学生在 `task_records` 中根本没有记录行 (Row)，原计划的 `UPDATE` 属于虚空扫表。且完全漏掉了 APP-02 提前开考的漏洞。
- **精准矫正执行**：
  1. **“三重门”拦截 (`task_entry` 改造)**：在 `backend/app/api/saas/member.py` 的进入考场入口处，加装三大硬核校验：
     - `if now < start_time` -> HTTP 403 未开始（彻底封死 APP-02 漏洞）。
     - `if now > deadline` -> HTTP 403 已结束（彻底封死 APP-05 过期强行参考）。
     - 校验现有记录 `status` 是否已经交卷。
  2. **缺考强制补行 (Lazy Insert-Select)**：废弃 UPDATE 方案。在触发懒扫表时，使用 `INSERT INTO task_records ... SELECT`，从 `sys_user` 中检索全校所有 role='member' 且 `NOT EXISTS` 于 `task_records` 的学生，强行插入 `status='absent', score=0` 的数据行。保证 B 端 `task_stats` (以表内记录数为分母) 逻辑无需修改任何代码，大盘数据瞬间精准。

### 3. RAG 知识库瘫痪：分词兜底与存量回填 (AI-RAG-01, P0)
- **过往误区**：误以为系统使用了会引发 OOM 的 `fastembed`，其实代码中走的是云端网关，只是错误配置了对话模型。且漏掉了最核心的 LIKE 检索 0 命中缺陷。
- **精准矫正执行**：
  1. **参数剥离**：在 `.env` 新增 `EMBEDDING_MODEL`，分离“对话”与“向量”模型，修改 `ai_service.py` 严格取用向量模型名调用 `/embeddings`。
  2. **兜底检索降维 (分词 OR 匹配)**：重写 `vector_store.py` 的 `_local_search`。彻底抛弃 `LIKE '%query[:12]%'` 的粗暴前缀匹配，引入基础分词或空格切割策略，将 SQL 拼接为多词组的 `OR` 模糊检索，确保私有化未配大模型 Token 时系统依旧可用。
  3. **存量数据洗警**：提供一次性脚本（或在 App 启动钩子中），扫描清理 `kb_chunks.embedding = '[]'` 的无效历史切片，重新回填向量数据。

### 4. 组卷幂等：利用天然锚点，化繁为简 (LLM-SYS-05, P0)
- **过往误区**：无脑要求加新字段，没有考虑到 AI 助手交互卡片流自带的状态天然屏障。
- **精准矫正执行**：
  1. **提取天然防重锚点 (`message_id`)**：在 `backend/app/api/saas/ai.py` 的 `execute_tool` 方法中，前端发送的卡片确认请求中必然包含唯一的 `message_id`。
  2. **状态短路防线**：以此为 Key，查验数据库（或通过记录缓存）中该卡片是否已被标记为 `executed`。若检测到重放双花，直接短路返回第一次成功创建的 `task_id` (HTTP 200 OK)。
  3. **(备用方案)** 如果脱离卡片流，使用传统表单端点仍需防护，则在数据表建 `UNIQUE(tenant_id, idempotency_key)` 索引，并在路由层通过 `try-except IntegrityError` 拦截 500 报错并转化为 200 返回。

---

## 结论
这份经过“千锤百炼”的 v1.5.1 修复档案，已经精确到了每一个文件路径和 SQL 动作。我们完美规避了数据库性能爆炸、反向代理限流误杀等严重二次灾难。

---

## 🏗️ 施工排雷指南 (Construction Guardrails)

在正式动刀代码前，必须严格遵照以下 6 条从实战源码中提炼出的“避坑细节”，方能实现真正的拿来即用：

1. **Nginx 限流的精确路由**：
   - 纠正：源码中并不存在 `/api/v1/saas/ai/` 路由。真正的 AI 高危网关前缀是 `/api/v1/admin/ai/` (B端) 和 `/api/v1/member/ai/` (C端)。
   - 施工：在 `nginx.conf` (`/etc/nginx/conf.d/default.conf`) 中，将 `map` 和 `limit_req_zone` 置于顶层，精准挂载到上述两个路由与 `/api/v1/auth/` 即可。
2. **缺考扫表的精准触发点**：
   - 补充：Lazy Sweep 不能靠空气触发。必须将其前置挂载到三大只读关键路径的中间件或前置依赖中：`member-tasks` (C端列表)、`task_stats` (B端统计)、`task_entry` (C端进考场)。
3. **缺考 `score=0` 的统计污染**：
   - 纠正：B 端 `task_stats` 目前是以 `score is not None` 来计算平均分。如果强行 `INSERT` 缺考记录且 `score=0`，会严重拉低全校均分，破坏统计逻辑。
   - 施工：缺考行插入时应置 `score = NULL`，仅用于 `absent` 人数统计；或在算均分时明确排除 `absent` 状态。
4. **C端状态机必须联动更新**：
   - 补充：虽然 B 端零修改，但 C 端 (`toc-new`) 的前端并未注册全新的 `absent` 状态！
   - 施工：必须在 `utils/format.ts:75` 状态字典和 `pages/index/index.vue:234` 的 `DONE_STATUSES` 数组中补上 `absent`，否则学生界面将显示英文代码或触发渲染异常。
5. **RAG 存量洗警的轻量化与解耦**：
   - 纠正：绝不能把存量回填放在 App 启动钩子里（会拖慢启动并浪费云配额）。必须提供一次性的 Python 脚本。
   - 施工：本地兜底搜索抛弃厚重的 `jieba` 分词。采用最轻量的“中文 Bigram (两字滑窗) OR 匹配” + “文件名优先”策略，足以应对无 Embedding 时的检索。
6. **幂等短路的原子化锁死**：
   - 纠正：“先 SELECT 查状态，再 UPDATE” 必定扛不住双击并发，照样双花。
   - 施工：在 `execute_tool` 中，查卡片标记时必须带上 `with_for_update()` 排他行锁；且因为 `message_id` 是可选参数，底层表结构的 `UNIQUE(tenant_id, idempotency_key)` 兜底方案也必须同步实施，缺一不可。

> 至此，这份 `v1.5repair-plan.md` 档案已实现 100% 闭环，可以直接作为最终的「开工图纸」移交施工！

---

## 🤖 附录：给 WorkBuddy 的“傻瓜式”代码清单 (Agent Runbook)

为了防止 AI 代理（如 WorkBuddy）在执行时发散或产生幻觉，以下是翻译自上述架构的**绝对精确**代码实现参考。施工代理必须严格照抄以下逻辑：

### 1. Nginx 真实配置 (`nginx.conf`)
必须将以下块写入 `nginx.conf` 顶层 `http` 内。注意：为了防止同校出口 IP 相同导致登录被大面积误杀，必须将 Auth（基于 IP，高频）与 AI（基于 JWT，低频）的限流 Zone 物理隔离！
```nginx
# AI 接口专用（基于 Token，防止单用户刷量）
map $http_authorization $ai_limit_key {
    default "";                  # 无 Token 不在此处限流，避免 IP 误杀
    "~^Bearer (.*)$" $1;         # 有 Token 则提取 JWT
}
limit_req_zone $ai_limit_key zone=ai_api_limit:10m rate=5r/m;

# Auth 登录专用（基于 IP，防爆破，阈值适当调高应对校园 NAT）
limit_req_zone $binary_remote_addr zone=auth_limit:10m rate=10r/s;

limit_req_status 429;
```
在 Server 块内高危路由挂载：
```nginx
location /api/v1/auth/ { limit_req zone=auth_limit burst=50 nodelay; ... }
location /api/v1/admin/ai/ { limit_req zone=ai_api_limit burst=2 nodelay; ... }
location /api/v1/member/ai/ { limit_req zone=ai_api_limit burst=2 nodelay; ... }
```

### 2. 缺考扫表精确 SQL (`member.py` 或 `tasks.py`)
在读取考卷前，执行以下原生 SQL（利用 SQLAlchemy 的 `session.execute`）：
```sql
INSERT INTO task_records (task_id, user_id, status, score, created_at, updated_at)
SELECT :task_id, id, 'absent', NULL, NOW(), NOW() 
FROM sys_user 
WHERE tenant_id = :tenant_id AND role = 'member' 
AND NOT EXISTS (
    SELECT 1 FROM task_records WHERE task_id = :task_id AND user_id = sys_user.id
);
```

### 3. C 端三重门 (`backend/app/api/saas/member.py` -> `task_entry`)
```python
if current_time < task.start_time:
    raise HTTPException(status_code=403, detail="考试尚未开始")
if current_time > task.end_time:
    raise HTTPException(status_code=403, detail="考试已结束")
# 接着再验证 record.status != 'submitted'
```

### 4. 幂等原子锁 (`backend/app/api/saas/ai.py` -> `execute_tool`)
```python
if message_id:
    # 必须加 with_for_update() 行锁防并发
    msg_record = db.query(Message).filter(Message.id == message_id).with_for_update().first()
    if msg_record and msg_record.status == 'executed':
        return {"code": 200, "data": {"task_id": msg_record.result_task_id}}
    # ... 执行完毕后标记 ...
    msg_record.status = 'executed'
    msg_record.result_task_id = new_task.id
```

### 5. RAG Bigram 兜底 (`backend/app/services/vector_store.py`)
```python
# 中文 2 字滑窗切词替代 jieba
q_clean = query.strip()
if len(q_clean) > 2:
    bigrams = [q_clean[i:i+2] for i in range(len(q_clean)-1)]
else:
    bigrams = [q_clean]

# 构建 SQLAlchemy OR 过滤
from sqlalchemy import or_
conditions = [KBChunk.content.ilike(f"%{bg}%") for bg in bigrams]
# ... filter(or_(*conditions)) ...
```
