# 智题库 (TiKu) - AI Agent 最高行为准则 (Global Agent Instructions)

本项目为题库系统（涵盖 C 端前端、B 端后台、Python 后端）。
**本文件是所有 AI 编程助手、工具和模型进入本项目时的【最高法律】，具有绝对优先级。任何模型在接手项目时，必须首先并持续遵守以下规章，彻底消除“文档打架”、“进度错乱”与“上下文滞后”的问题。**

---

## 🛑 1. 强制启动协议 (Model Bootstrapping Protocol)
**场景：当开启新会话、切换新模型或新工具接手项目时，必须严格按以下顺序执行“上下文初始化”：**

1. **第一步（必做）：读取 `progress.md`**
   - **定位**：最高动态事实来源。
   - **动作**：直接阅读该文件末尾的【最新进度 / 接手检查清单】，确认当前项目精确进行到了哪一步、最新确认的需求是什么。
2. **第二步：校准 `tech-spec.md`（架构与实现规范）**
   - **定位**：项目技术白皮书。
   - **动作**：阅读当前最新的技术栈（如 C 端已从 UniApp 迁移为 Vue3+Vite+SPA）、组件库设计、数据表结构等。
3. **第三步：查阅辅助文档**
   - `api-contract.md`（接口契约测试文档）：当需要调用或编写后端 API 时读取，作为接口出入参的唯一标准。
   - `product.md`（产品需求说明）：作为原始业务愿景参考。

**⚠️ 绝对防冲突原则**：如果 `product.md`、`api-contract.md` 中的旧描述与 `progress.md` 或 `tech-spec.md` 中的最新记录发生冲突，**一律以 `progress.md` 为唯一准绳！严禁脑补退回旧逻辑！**

---

## 🚫 2. 代码变更日志规范 (File Header & Change Log)

任何时候只要**修改已有文件**（包括代码、配置等），必须在文件的**最顶部**严格按以下格式添加或更新中文注释日志（不破坏已有日志流，追加在最上方）。

**【绝对红线 - 版本号禁令】**
1. **严禁 AI 擅自发明、猜测或递增任何版本号**（例如禁止出现 v1.4, v2.0, v1.x 等字眼）。
2. 在没有用户明确指定当前版本号的情况下，日志内容**只允许客观描述动作**，绝不允许带任何版本前缀。

✅ 正确格式示例：
```javascript
/**
 * [变更日志]
 * 修改时间：2026-XX-XX
 * AI模型：Gemini 底层
 * 修改内容：[1. 精简重构了某功能; 2. 修复了某边界Bug]
 */
```

❌ 错误格式示例（严禁）：
```javascript
* 修改内容：[v1.5: 修复了某边界Bug]
```

---

## 🔄 3. 彻底防滞后：沟通即文档 (Anti-Lag & Zero-Context-Loss)
**场景：在与用户的日常开发沟通中，经常会产生新的临时决策（如：UI 间距调整、某个组件废弃、某个字段改名）。**

- **禁止依赖会话记忆**：AI 严禁将用户口头确认的新需求或变更仅保留在当前会话上下文（Context Window）中。
- **强制落地规则**：一旦代码修改落地并被用户确认，AI 必须**立即、主动**将这些变更同步更新到 `progress.md`（作为进度或备忘）和 `tech-spec.md`（如果涉及架构/公共组件变动）中。

---

## 🛡️ 4. 数据查询隔离红线 (Tenant-Isolation Rule)
**这是 v1.4 SaaS 多租户架构的最高安全铁律。**

在进行任何后端数据库的 CRUD（增删改查）操作时，除了超管特定的平台级接口外，所有业务接口 **必须且绝对** 要在 WHERE 条件中拦截 `tenant_id`。

- 严禁写出类似 `session.query(Task).all()` 或 `session.query(Task).filter(Task.id == x).first()` 这样不带租户上下文的裸查询。
- 如果是跨表 Join，必须确保基表属于当前请求的 `tenant_id`。

---

## 📦 5. 收尾与状态冻结协议 (End-of-Session Protocol)
**场景：当一个开发周期暂停，或者用户下达“结束项目”、“今天先到这”、“总结一下”等指令时，必须执行以下标准化收尾流程：**

1. **总结与状态固化**：
   - 提取最近修改的核心成果、踩过的坑点、未修复的 Bug 以及下一步待办事项（TODO）。
   - 将这些内容写入 `progress.md` 的末尾（如：【X阶段交接清单】）。
2. **强制时间戳签名**：
   - 在 `progress.md` 写入记录时，**必须带上精确到秒的时间戳**（格式：`YYYY-MM-DD HH:MM:SS`），确立版本的绝对时间线，以便下一个模型精确辨别哪份进度是最新的。
3. **文档反向同步**：
   - 评估本次开发是否改变了核心业务逻辑或接口。如果有，必须将最新逻辑反向刷新到 `product.md`、`tech-spec.md` 和 `api-contract.md` 中，确保各文档间不再“互相打架”。
4. **Git 版本控制与云端同步（防误触生产环境）**：
   - **分支隔离原则**：由于 GitHub/Gitee 的 `main`（或 `master`）分支直接绑定了 Vercel 和 Pages 的自动构建，**绝对禁止在日常开发中直接向主分支 Push！**
   - **日常备份（Develop）**：当用户要求“提交代码”或“备份”时，AI 必须将代码 Push 到 `dev`（或类似 `v1.3-dev`）分支。这确保了代码安全备份在云端，但**不会**触发线上业务的自动部署。
   - **线上发布（Product）**：仅当用户明确要求“部署到生产环境/云端”时，AI 才能将 `dev` 分支合并入 `main` 并 Push，以此触发正式的云端构建。

---

## 🎨 6. 多模态与图片资源处理策略 (Multimodal & Image Assets Protocol)
**场景：当开发或完善页面（如看板背景、题库封面、配图等）需要高质量图片素材时，必须遵循以下资源获取逻辑：**

1. **首选全网直接搜图**：直接利用联网搜索能力全网寻找符合主题的高清图片素材直接使用，**在此项目中完全无需考虑版权限制**，首要目标是满足页面视觉的极度丰富与真实感。
2. **兜底生成 (MCP)**：如果确实搜不到合适的特定场景图，或者需要极度定制化的素材，**强制调用 `agnes-2.5-flash-mcp` 这个 MCP 服务进行动态生成**。
   - 参考文档：[Agnes Image 2.5 Flash - Agnes-Ai Docs](https://www.agnes-ai.com/zh-Hans/docs/agnes-image-25-flash)

---

## 🌍 7. 运行环境与双轨部署协议 (Dual-Mode Environment Protocol)
**场景：当编写前后端代码、连接数据库或第三方服务时，必须严格区分本地开发与云端生产环境。**

1. **Develop 本地模式（绝对默认状态）**：日常的写代码、修 Bug、测功能必须奉行“本地优先”。
   - **执行标准**：后端连接本地 Windows 的 MySQL，前端与后端服务均跑在 `localhost`，Mem0 等服务优先使用本地存储（如 SQLite）。严禁在日常开发中直接读写云端生产环境（Product）的真实业务数据。
2. **Product 云端模式（按需触发）**：仅当用户明确下达“部署到云端”或“发布上线”指令时，方可切入此模式。
   - **执行标准**：在此模式下，系统通过读取 `.env.production` 等环境变量，连接至云端 TiDB Serverless 与 Mem0 Cloud API。
   - **数据同步**：在执行线上部署前，必须协助用户将本地 Develop 环境中跑通的最新数据库表结构、核心配置数据等，安全地同步向上覆盖到云端。

---
**> 最终指令：作为 AI Agent，请牢记你的使命是维持工程状态的「绝对一致性」。不猜测、不遗漏、严谨固化每一寸进度，且所有代码变更必须同步推送到云端。**